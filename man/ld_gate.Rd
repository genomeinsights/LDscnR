% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/ld_region_scan.R
\name{ld_gate}
\alias{ld_gate}
\title{The background gate -- check a null before reading any p-value from it}
\usage{
ld_gate(null, edges, tau = 0.05, l_min = 3L, warn_at = 0.5)
}
\arguments{
\item{null}{An \code{ld_null} object (\code{\link[=ld_null_from_p]{ld_null_from_p()}} or \code{\link[=structured_null]{structured_null()}}),
or a \strong{named list} of them to tabulate several bases at once.}

\item{edges}{An \code{\link[=ld_edges]{ld_edges()}} object. When \code{null} is a list, build one edge
graph over the union of their \code{universe}s and pass it here: edge membership
is a property of a marker pair, not of the marker set, so a shared graph is
what makes the rows comparable.}

\item{tau}{C-score threshold at which regions are formed (default 0.05).}

\item{l_min}{Minimum region size in markers (default 3).}

\item{warn_at}{Fraction of the observed region count at which a basis is
flagged as failing the gate (default 0.5).}
}
\value{
A \code{data.table}, one row per basis, with the observed counts, the
median/IQR/max per-surrogate counts, \code{frac_surr_any_region}, the
median-to-observed ratio, and a logical \code{pass}.
}
\description{
For each null bundle, the \strong{median per-surrogate} count of markers with
\code{C > 0} and of LD-regions of at least \code{l_min} markers, alongside the observed
counts. A basis whose surrogates approach the observed counts is mis-specified:
it reproduces the very signal it is supposed to be a null for, and its
p-values are not interpretable. Run this \emph{before} \code{\link[=ld_region_scan]{ld_region_scan()}}, and
report it -- a mis-specified null that is disclosed is a result, whereas one
that is quietly dropped is not.
}
\details{
Report the median per surrogate, never a count pooled across surrogates: a
pooled total confounds "every surrogate is mildly noisy" with "one surrogate
exploded", and the two have opposite implications. For the same reason the
returned table carries \code{q3_regions}, \code{max_regions} and \code{frac_surr_any_region}
next to the median -- permutation nulls are often heavy-tailed, silent in four
surrogates out of five and explosive in the fifth, and a median alone reports
that as "silent".
}
\seealso{
\code{\link[=ld_region_scan]{ld_region_scan()}}, \code{\link[=ld_null_from_p]{ld_null_from_p()}}
}
