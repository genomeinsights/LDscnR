% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/ld_scan.R
\name{ld_scan}
\alias{ld_scan}
\title{LD-aware outlier regions from your own scan, end to end}
\usage{
ld_scan(
  p_obs,
  p_perm,
  ld_ws,
  map,
  GTs,
  decay_sum,
  tau = 0.05,
  l_min = 3L,
  fdr = 0.05,
  alpha = 0.05,
  rho = colnames(ld_ws),
  qstar = seq(0, 0.95, by = 0.05),
  rho_ld = 0.9,
  dcap = 5e+05,
  rho_d = NULL,
  B = NULL,
  c2 = TRUE,
  tau_grid = seq(0.02, 0.5, by = 0.02),
  lmin_grid = c(1L, 2L, 3L, 5L, 10L, 15L, 20L),
  basis = "user-supplied",
  engine = "user-supplied",
  cores = 1L,
  verbose = TRUE
)
}
\arguments{
\item{p_obs}{Observed per-SNP p-values, aligned to the rows of \code{ld_ws}.}

\item{p_perm}{Surrogate p-values: a SNPs-by-\code{B} matrix, a list of vectors, or a
\verb{function(b)} streaming them from disk (see \code{\link[=ld_null_from_p]{ld_null_from_p()}}).}

\item{ld_ws}{Local-LD support matrix (SNPs x \code{ld_w} windows; see \code{\link[=compute_ld_w]{compute_ld_w()}}).}

\item{map}{data.frame/data.table with \code{marker}, \code{Chr}, \code{Pos}.}

\item{GTs}{Genotype dosage matrix (individuals x SNPs, column names = markers),
used only to compute the r^2 between markers for the edge graph.}

\item{decay_sum}{Per-chromosome LD-decay summary (see \code{\link[=compute_LD_decay]{compute_LD_decay()}}).}

\item{tau}{C-score threshold for forming regions (default 0.05).}

\item{l_min}{Minimum region size in markers (default 3).}

\item{fdr}{BH level for \code{q_R} (default 0.05).}

\item{alpha, rho, qstar}{C-score parameters (see \code{\link[=ld_cscore]{ld_cscore()}}).}

\item{rho_ld, dcap, rho_d}{Clustering parameters (see \code{\link[=ld_edges]{ld_edges()}}).}

\item{B}{Number of surrogates; required only if \code{p_perm} is a function.}

\item{c2}{Compute the second-tier C-score ranking (default \code{TRUE}). This
re-runs the region test over the whole grid and is by far the most expensive
step; set \code{FALSE} for a quick look.}

\item{tau_grid, lmin_grid}{The \code{C^(2)} grid (see \code{\link[=ld_region_c2]{ld_region_c2()}}).}

\item{basis, engine}{Labels describing how surrogates were built and which
engine produced the p-values. Documentation, carried through the output.}

\item{cores}{Cores for reducing surrogates to C-scores (Unix only).}

\item{verbose}{Print progress.}
}
\value{
An \code{ld_scan} object with \code{gate}, \code{regions} (the \code{ld_region_scan}),
\code{c2} (or \code{NULL}), \code{null}, \code{edges} and \code{params}.
}
\description{
The one-call entry point. Give it per-SNP p-values for your observed data and
for \code{B} permuted datasets, and it runs the whole pipeline: reduce both to
C-scores (\code{\link[=ld_null_from_p]{ld_null_from_p()}}), build the LD-edge graph (\code{\link[=ld_edges]{ld_edges()}}), check
the null against the observed background (\code{\link[=ld_gate]{ld_gate()}}), call regions and test
each at its own locus (\code{\link[=ld_region_scan]{ld_region_scan()}}), and -- unless you turn it off --
rank them by the second-tier C-score over the \verb{(tau, l_min)} grid
(\code{\link[=ld_region_c2]{ld_region_c2()}}).
}
\details{
It is engine-agnostic by construction. The pipeline never sees your phenotype
or your association model, only p-values, so EMMAX, LFMM, BayPass, a plain GLM
or anything else all work, and two engines can be compared on identical
footing. The one requirement is that \strong{every surrogate is scanned with the same
engine and settings as the observed data} -- otherwise the comparison is not
like-for-like and nothing downstream can detect it.

How you permute is the scientific choice, and it is yours. The surrogates
define what "no signal" means: shuffling labels within localities holds
between-locality composition fixed and therefore treats a clinal signal as
null, whereas shuffling globally breaks the cline and treats it as real. Decide
which hypothesis you are testing, permute accordingly, and state it -- the
\code{basis} argument is carried through every printed output for exactly that
reason.

The result prints the \strong{gate before the regions}, deliberately. A p-value from
a null whose surrogates reproduce the observed background is not interpretable,
and no amount of downstream care recovers it.
}
\examples{
\dontrun{
## p_obs: your observed scan. p_list: one p-value vector per permuted dataset,
## each produced by re-running THAT SAME scan on permuted labels.
fit <- ld_scan(p_obs, p_list, ld_ws, map, GTs, decay_sum,
               tau = 0.05, l_min = 3, rho_ld = 0.6,
               basis = "within-locality permutation", engine = "EMMAX")
fit                          # gate first, then the significant regions
fit$gate                     # is this null usable at all?
fit$regions$regions          # per-region s_R, p_R, q_R
fit$c2$regions               # the same regions ranked by C^(2)
}
}
\seealso{
\code{\link[=ld_null_from_p]{ld_null_from_p()}}, \code{\link[=ld_gate]{ld_gate()}}, \code{\link[=ld_region_scan]{ld_region_scan()}}, \code{\link[=ld_region_c2]{ld_region_c2()}}
}
