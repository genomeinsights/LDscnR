% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/ld_benchmark.R
\name{evaluate_ors}
\alias{evaluate_ors}
\title{Evaluate outlier regions against ground truth (dedup-neutral)}
\usage{
evaluate_ors(regions, map, qtn_ld_table, r2_match, d_match)
}
\arguments{
\item{regions}{List of member-marker vectors.}

\item{map}{data.table from \code{\link[=flag_true_qtns]{flag_true_qtns()}}.}

\item{qtn_ld_table}{Output of \code{\link[=qtn_ld_table]{qtn_ld_table()}}.}

\item{r2_match, d_match}{Match thresholds (\code{\link[=score_thresholds]{score_thresholds()}}).}
}
\value{
A list: \code{TP}, \code{FP}, \code{FN}, \code{extras} (dedup losers, uncounted),
\code{Precision}, \code{Recall}, \code{PR} (= Precision x Recall).
}
\description{
Scores a set of outlier regions into TP / FP / recall / precision on the QTN
truth. \strong{Dedup-neutral}: a region that matches an already-claimed
true-positive QTN (a duplicate for the same QTN, from over-fragmentation) is
counted as \emph{neither} TP nor FP -- it is dropped. This removes the
clustering-parameter sensitivity of the metric (splitting one true region into
N pieces still yields 1 TP and 0 extra FP), while genuine false regions still
count and over-merging is still penalised through recall (one region can claim
only one QTN). This makes it a fair, method-agnostic comparison of outlier
callers on a common OR frame.
}
\seealso{
\code{\link[=classify_ors]{classify_ors()}}, \code{\link[=pr_auc]{pr_auc()}}, \code{\link[=score_thresholds]{score_thresholds()}}
}
