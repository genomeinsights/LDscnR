% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/ld_benchmark.R
\name{classify_ors}
\alias{classify_ors}
\title{Classify each outlier region as true or false positive}
\usage{
classify_ors(regions, map, qtn_ld_table, r2_match, d_match)
}
\arguments{
\item{regions}{List of member-marker vectors (e.g. \code{ld_outlier_regions()$regions}).}

\item{map}{data.table from \code{\link[=flag_true_qtns]{flag_true_qtns()}} (\code{type}, \code{true_pos_QTN}, \code{marker}).}

\item{qtn_ld_table}{Output of \code{\link[=qtn_ld_table]{qtn_ld_table()}}.}

\item{r2_match, d_match}{Match thresholds (\code{\link[=score_thresholds]{score_thresholds()}}).}
}
\value{
A data.table: \code{CL_id}, \code{n_loci}, \code{qtn} (claimed QTN or \code{NA}),
\code{evidence} (r^2), \code{is_TP}.
}
\description{
Assigns every region its focal QTN (nearest in r^2 within the match
thresholds), resolves duplicates (each QTN can be claimed by only one region --
the best-supported one), and flags whether the claimed QTN is a true positive.
One row per region -- the table an OR-level Manhattan plot needs.
}
