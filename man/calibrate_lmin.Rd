% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/structured_null.R
\name{calibrate_lmin}
\alias{calibrate_lmin}
\title{Suggest l_min from the structured null (data-driven region-size floor)}
\usage{
calibrate_lmin(bundle, edges, tau = 0.05, q = 0.99)
}
\arguments{
\item{bundle}{An \code{ld_null} object from \code{\link[=structured_null]{structured_null()}}.}

\item{edges}{An \code{\link[=ld_edges]{ld_edges()}} object over \code{bundle$universe}.}

\item{tau}{C-score threshold at which to measure null clustering (default 0.05).}

\item{q}{Upper quantile of the null max-region-size distribution (default 0.99).}
}
\value{
Integer \code{l_min} = \verb{1 + floor(quantile(null max region sizes, q))}.
}
\description{
The region-size filter \code{l_min} set \emph{by the null} instead of by hand: the
smallest region size that structure-only surrogates essentially never reach.
For each surrogate its C-score-significant markers (at threshold \code{tau}) are
clustered and the size of its largest region recorded; \code{l_min} is one more than
the upper-\code{q} quantile of that null max-size distribution. A region at least
this large is therefore implausible under the structure null -- the exact logic
\code{l_min} encodes, made data-driven.
}
\details{
The null bundle is \code{l_min}-free (and \code{tau}-free), so this reuses the surrogate
C-scores already computed by \code{\link[=structured_null]{structured_null()}} at no extra cost. \code{tau} is the
C-threshold at which the null's clustering propensity is read: use a \emph{permissive}
value (default \code{0.05}) so \code{l_min} reflects how large chance clusters can grow
when many markers are let through -- the regime \code{l_min} must guard, and what then
lets \code{\link[=calibrate_tauc]{calibrate_tauc()}} pick a lower, more powerful \code{tau_C}.
}
\seealso{
\code{\link[=calibrate_tauc]{calibrate_tauc()}}, \code{\link[=null_fdr]{null_fdr()}}, \code{\link[=structured_null]{structured_null()}}
}
