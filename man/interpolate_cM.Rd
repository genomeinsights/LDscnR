% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/compute_ld_structure.R
\name{interpolate_cM}
\alias{interpolate_cM}
\title{Interpolate Genetic Map Position (cM)}
\usage{
interpolate_cM(genetic_map, Chr, Pos)
}
\arguments{
\item{genetic_map}{A data.table/data.frame with \code{Chr}, \code{Pos}, and \code{cM}
columns (at least 2 rows per chromosome to interpolate). Any
chromosome-naming differences from \code{Chr} below (e.g. a genetic map
using \code{"chromosome_1"} instead of \code{"Chr1"}) must be reconciled by the
caller before calling this function.}

\item{Chr}{Character vector of chromosome names to query.}

\item{Pos}{Numeric vector of physical positions to query, same length
and order as \code{Chr}.}
}
\value{
Numeric vector of interpolated cM positions, same length/order
as \code{Chr}/\code{Pos}. Positions outside a chromosome's mapped range are
clamped to the nearest endpoint's cM value (\code{rule = 2} in
\code{\link[stats:approx]{stats::approx()}}) -- fine for markers just past the map's first/last
point, but a chromosome entirely absent from \code{genetic_map}, or with
fewer than 2 points, returns \code{NA} for all its positions.
}
\description{
Linearly interpolates centimorgan (cM) position from a genetic map, for
arbitrary physical positions. Converts physical (bp) distance between
markers into genetic (cM) distance -- a much more direct measure of
recombination probability than physical distance, which can be
misleading wherever recombination rate itself varies (e.g. across a
low-recombination block like a centromere or inversion versus the
surrounding chromosome). Checked directly on real data: some marker
pairs over 1 Mb apart physically sat at cM distance 0 (fully linked, no
measurable recombination between them), while others only a few hundred
kb apart already spanned several cM.
}
\examples{
\dontrun{
genetic_map <- data.table::data.table(
  Chr = "Chr1", Pos = c(0, 1e6, 2e6), cM = c(0, 5, 20)
)
interpolate_cM(genetic_map, Chr = "Chr1", Pos = c(5e5, 1.5e6))
}

}
