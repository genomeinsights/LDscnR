% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/ld_outlier_scan.R
\name{ld_outlier_scan}
\alias{ld_outlier_scan}
\title{LD-aware outlier regions from stage-1 clusters, end to end}
\usage{
ld_outlier_scan(
  stage1,
  map,
  p_obs,
  p_perm = NULL,
  statistic = c("simes", "unit"),
  size_floor = 8L,
  alpha = 0.05,
  assembly = c("stage2_discovered", "physical"),
  GTs = NULL,
  LD_decay = NULL,
  score_threshold = 0.8,
  distance_threshold = 1e+05,
  gap = 3e+05,
  annotation = NULL,
  chrom_lengths = NULL,
  n_rotations = 10000L,
  rotation_scheme = c("within", "genome"),
  B = NULL,
  perm_level = c("units", "regions"),
  cores = 1L,
  verbose = TRUE
)
}
\arguments{
\item{stage1}{An \code{ld_complexity_reduction} object (must have \verb{$params$rho}).}

\item{map}{data.frame/data.table with \code{marker}, \code{Chr}, \code{Pos}, aligned to
\code{stage1}'s marker universe.}

\item{p_obs}{Per-marker p-values (if \code{statistic = "simes"}), aligned to
\code{map$marker}, or per-unit p-values in unit order (if \code{statistic = "unit"},
aligned to the units \code{\link[=ld_unit_matrix]{ld_unit_matrix()}} would return at the same
\code{size_floor}).}

\item{p_perm}{Optional surrogate p-values (same shape \code{p_obs} had) --
supplying this runs \code{\link[=ld_outlier_perm]{ld_outlier_perm()}} and adds \verb{$null} to the result.
\code{NULL} (default) skips it.}

\item{statistic}{\code{"simes"} or \code{"unit"}.}

\item{size_floor}{Minimum markers per tested unit (default 8L).}

\item{alpha}{BH level (default 0.05).}

\item{assembly}{\code{"stage2_discovered"} (default) -- re-run
\code{\link[=ld_prune_and_eMLG]{ld_prune_and_eMLG()}} over the significant clusters only, so a region can
only span discovered signal -- or \code{"physical"}, a plain merge of
significant clusters within \code{gap} on a chromosome. The two are not
equivalent in general; \code{"physical"} is retained as a near-free check
(agreement between an arbitrary rule and a motivated one is evidence
neither is an artefact of the other), not as an equally-preferred
alternative.}

\item{GTs, LD_decay}{Required only when \code{assembly = "stage2_discovered"}.}

\item{score_threshold, distance_threshold}{\code{\link[=ld_prune_and_eMLG]{ld_prune_and_eMLG()}} parameters
for \code{"stage2_discovered"}. \code{ld_w_threshold} is fixed at 0 internally, not
exposed: used only to merge already-significant clusters it is not a
gate, only a speed filter, and 0 filters nothing.}

\item{gap}{Physical merge distance in bp, used only when
\code{assembly = "physical"}.}

\item{annotation}{Optional bed-like data.table -- supplying this (with
\code{chrom_lengths}) runs \code{\link[=ld_region_rotation]{ld_region_rotation()}} on the assembled regions and
adds \verb{$rotation} to the result. \code{NULL} (default) skips it.}

\item{chrom_lengths}{Required if \code{annotation} is supplied.}

\item{n_rotations, rotation_scheme}{Passed to \code{\link[=ld_region_rotation]{ld_region_rotation()}}.}

\item{B, perm_level, cores}{Passed to \code{\link[=ld_outlier_perm]{ld_outlier_perm()}}.}

\item{verbose}{Print progress (default \code{TRUE}).}
}
\value{
An \code{ld_outlier_scan} object: \code{test} (the \code{ld_outlier_test} result --
\code{units}, \code{regions}), \code{null} (the \code{ld_outlier_perm} result, or \code{NULL}),
\code{rotation} (the \code{ld_region_rotation} result, or \code{NULL}), \code{params}.
}
\description{
The sibling entry point to \code{\link[=ld_scan]{ld_scan()}}, composing \code{\link[=ld_outlier_test]{ld_outlier_test()}},
\code{\link[=ld_outlier_perm]{ld_outlier_perm()}} and \code{\link[=ld_region_rotation]{ld_region_rotation()}} the same way \code{\link[=ld_scan]{ld_scan()}}
composes its own C-score sub-functions -- for one-call convenience, while
each piece stays independently usable for anyone who wants to reuse region
assembly on a different region set, or run the rotation null on regions from
elsewhere.
}
\details{
This is a genuinely different method from \code{\link[=ld_scan]{ld_scan()}}, not an alternative
parameterisation of it: that one computes a consistency C-score (Fang et al.
2021) over an \code{ld_w} quantile grid and thresholds it; this one tests
\code{\link[=ld_complexity_reduction]{ld_complexity_reduction()}} clusters directly (Simes over members, or your
own test on an \code{\link[=ld_unit_matrix]{ld_unit_matrix()}} variable), BH-corrects, and assembles
significant units into regions. Both turn p-values plus LD structure into
significant regions; pick one rather than have it picked for you.
}
\seealso{
\code{\link[=ld_scan]{ld_scan()}}, \code{\link[=ld_unit_matrix]{ld_unit_matrix()}}, \code{\link[=ld_outlier_test]{ld_outlier_test()}},
\code{\link[=ld_outlier_perm]{ld_outlier_perm()}}, \code{\link[=ld_region_rotation]{ld_region_rotation()}}
}
