% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/ld_outlier_clusters.R
\name{rmsc_threshold}
\alias{rmsc_threshold}
\title{Rejection-maximising selection of a local-LD threshold}
\usage{
rmsc_threshold(pval, ld_w, fdr = 0.05, grid = seq(0, 0.98, by = 0.01))
}
\arguments{
\item{pval}{Numeric vector of per-SNP association p-values (from any method).}

\item{ld_w}{Numeric vector of per-SNP local LD support (\code{\link[=compute_ld_w]{compute_ld_w()}}),
aligned to \code{pval}.}

\item{fdr}{SNP-level false-discovery rate used when counting discoveries.}

\item{grid}{Grid of \code{ld_w} quantile thresholds to scan.}
}
\value{
A list with \code{grid}, \code{rejections} (discoveries at each threshold),
\code{q_star} (the rejection-maximising quantile) and \code{no_elbow} (TRUE if
filtering never beats keeping all SNPs, i.e. the prioritisation does not
apply to these data).
}
\description{
Independent-filtering (Bourgon et al. 2010) choice of an \code{ld_w} quantile
threshold: over a grid of quantiles, retain the SNPs above each, FDR-correct
their association p-values, and return the threshold that maximises the number
of discoveries. Because \code{ld_w} is computed from LD structure alone it is
approximately independent of the association p-value under the null, which is
the condition independent filtering requires.
}
\references{
Bourgon R, Gentleman R, Huber W (2010). Independent filtering
increases detection power for high-throughput experiments. \emph{PNAS}
107:9546-9551.
}
