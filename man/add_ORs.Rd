% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/manhattan.R
\name{add_ORs}
\alias{add_ORs}
\title{Add outlier-region labels to a SNP map}
\usage{
add_ORs(
  gds,
  ld_decay,
  map,
  stat = "Joint_C",
  sign_th,
  sign_if = "greater",
  mode,
  rho_d,
  rho_ld,
  l_min = 1
)
}
\arguments{
\item{gds}{Open GDS object.}

\item{ld_decay}{Object of class \code{"ld_decay"}.}

\item{map}{SNP annotation table containing at least \code{marker},
\code{Chr}, and the selected statistic column.}

\item{stat}{Name of the statistic column in \code{map} used for OR detection.}

\item{sign_th}{Significance threshold used to classify outlier SNPs.}

\item{sign_if}{Direction of significance; either \code{"greater"} or
\code{"less"}.}

\item{mode}{OR detection mode passed to \code{detect_or()}.}

\item{rho_d}{Relative distance threshold used for OR detection.}

\item{rho_ld}{Relative LD threshold used for OR detection.}

\item{l_min}{Minimum number of SNPs required for an outlier region.}
}
\value{
An updated \code{data.table} with an added \code{OR_id} column.
}
\description{
Detects outlier regions from a selected SNP-level statistic and merges the
resulting region labels into a SNP map.
}
\details{
SNPs not assigned to any outlier region are labelled \code{"ns"}.

The function first restricts LD estimation to SNPs exceeding the supplied
threshold, computes the corresponding LD edge list, detects outlier regions,
and then merges the resulting region assignments back into the full map.
}
