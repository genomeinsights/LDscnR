% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/ld_cscore.R
\name{ld_cscore}
\alias{ld_cscore}
\title{Per-SNP consistency C-score}
\usage{
ld_cscore(
  p,
  ld_ws,
  alpha = 0.05,
  rho = colnames(ld_ws),
  qstar = seq(0, 0.95, by = 0.05)
)
}
\arguments{
\item{p}{Numeric vector of per-SNP association p-values (from any method),
aligned to the rows of \code{ld_ws}.}

\item{ld_ws}{Numeric matrix of local-LD support: SNPs in rows, \code{ld_w} windows
(\code{rho}) in columns, \code{rownames} = markers. See \code{\link[=compute_ld_w]{compute_ld_w()}}.}

\item{alpha}{Within-candidate BH-FDR level (fixed scalar; default 0.05). A
vector adds an extra integration axis (robustness check only).}

\item{rho}{\code{ld_w} window columns of \code{ld_ws} to integrate over (default: all).}

\item{qstar}{Local-LD quantile thresholds to integrate over
(default \code{seq(0, 0.95, by = 0.05)}).}
}
\value{
Named numeric vector of per-SNP C-scores in \verb{[0, 1]}
(\code{names} = markers). Gate with a threshold \code{tau_C}
(\code{names(C)[C >= tau_C]}) calibrated by \code{\link[=calibrate_tauc]{calibrate_tauc()}}.
}
\description{
The LD-scan C-score: for each SNP, the fraction of \verb{(rho, q*)} cells in which
it is BOTH a local-LD candidate (its \code{ld_w} at window \code{rho} exceeds the \verb{q*}
quantile of that window) AND significant (Benjamini-Hochberg FDR \verb{< alpha}
among the candidates of that cell). It integrates over the two nuisance
parameters that have a natural \verb{[0, 1]} domain -- the \code{ld_w} window \code{rho} and
the local-LD quantile \verb{q*} -- at a FIXED significance level. A high C means a
SNP is consistently prioritised-and-significant however the LD window and
stringency are set, concentrating on true, LD-supported signal.
}
\details{
\code{alpha} is fixed (default 0.05) rather than swept: \code{rho} and \verb{q*} span \verb{[0, 1]}
so integrating over them is assumption-free, but \code{alpha} has no natural range,
and the structure-aware null (\code{\link[=structured_null]{structured_null()}}) prices any
anticonservativeness into the \code{tau_C} threshold -- so sweeping \code{alpha} is
unnecessary (and empirically neutral). Passing a \emph{vector} \code{alpha} folds it into
the integration too (an extra sweep axis), which is useful only as a
robustness check that the fixed choice does not matter; keep the default for
analysis.

The C-score is a per-SNP statistic: clustering into outlier regions
(\code{\link[=ld_regions]{ld_regions()}}) and the region-size filter \code{l_min} are applied downstream,
not folded in.
}
\seealso{
\code{\link[=ld_regions]{ld_regions()}}, \code{\link[=structured_null]{structured_null()}}, \code{\link[=calibrate_tauc]{calibrate_tauc()}}
}
