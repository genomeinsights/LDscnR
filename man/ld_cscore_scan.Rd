% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/ld_region_stability.R
\name{ld_cscore_scan}
\alias{ld_cscore_scan}
\title{Build the C-score and LD-edge graph for a region scan}
\usage{
ld_cscore_scan(
  y = NULL,
  GTs,
  K = NULL,
  ld_ws,
  map,
  decay_sum,
  alpha = 0.05,
  rho = colnames(ld_ws),
  qstar = seq(0, 0.95, by = 0.05),
  rho_ld = 0.9,
  dcap = 5e+05,
  rho_d = NULL,
  prep = NULL,
  pvals = NULL
)
}
\arguments{
\item{y}{Phenotype vector (length = individuals). Ignored if \code{pvals} is given.}

\item{GTs}{Genotype dosage matrix (individuals x SNPs; column names = markers).}

\item{K}{Kinship / GRM for EMMAX. Not needed when \code{pvals} or \code{prep} is supplied.}

\item{ld_ws}{Local-LD support matrix (SNPs x \code{ld_w} windows; see \code{\link[=ld_cscore]{ld_cscore()}}).}

\item{map}{data.frame/data.table with \code{marker}, \code{Chr}, \code{Pos} (aligned to \code{GTs}).}

\item{decay_sum}{Per-chromosome LD-decay summary (see \code{\link[=ld_edges]{ld_edges()}}).}

\item{alpha, rho, qstar}{C-score parameters passed to \code{\link[=ld_cscore]{ld_cscore()}}.}

\item{rho_ld, dcap, rho_d}{Clustering parameters passed to \code{\link[=ld_edges]{ld_edges()}}.}

\item{prep}{Optional \code{emmax_prep} from \code{\link[=emmax_setup]{emmax_setup()}} to reuse.}

\item{pvals}{Optional precomputed per-SNP p-values (skips EMMAX).}
}
\value{
A list with \code{C} (full per-SNP C-score), \code{edges} (an \code{\link[=ld_edges]{ld_edges()}}
object over the \code{C > 0} universe), and \code{prep} (the \code{emmax_prep}, or \code{NULL}
when \code{pvals} was supplied). Feed \code{C} and \code{edges} to \code{\link[=ld_region_stability]{ld_region_stability()}}.
}
\description{
The data-prep step for \code{\link[=ld_region_stability]{ld_region_stability()}} (and for ad-hoc clustering at
any \code{tau_C} / \code{l_min}): from a phenotype (or a precomputed association
p-vector) it returns the full per-SNP consistency C-score (\code{\link[=ld_cscore]{ld_cscore()}}) and
the cached LD r^2-edge graph (\code{\link[=ld_edges]{ld_edges()}}). Unlike \code{\link[=ld_outlier_regions]{ld_outlier_regions()}} it
draws no structure-aware null and commits to no threshold -- it just
assembles the two objects a threshold-free region ranking needs.
}
\details{
The edge graph is deliberately built over the \strong{entire} lit-up universe
(\code{names(C)[C > 0]}), not a gated subset, so that a downward \code{tau_C} sweep
never references a marker missing from the graph.

Two entry points:
\itemize{
\item supply \code{y} (and \code{K} or \code{prep}) to run a fast EMMAX scan for the p-values;
\item or supply \code{pvals} directly (e.g. LFMM p-values) to skip EMMAX -- then \code{K}
is not needed, though \code{GTs} is still required for the LD graph.
}

\code{pvals} / the EMMAX output must be row-aligned to \code{ld_ws} (and to the columns
of \code{GTs} and rows of \code{map}), exactly as elsewhere in the package.
}
\seealso{
\code{\link[=ld_region_stability]{ld_region_stability()}}, \code{\link[=ld_cscore]{ld_cscore()}}, \code{\link[=ld_edges]{ld_edges()}},
\code{\link[=ld_outlier_regions]{ld_outlier_regions()}}
}
