% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/ld_prune_and_eMLG.R
\name{ld_prune_and_eMLG}
\alias{ld_prune_and_eMLG}
\title{Combined LD-pruning + eMLG generation via distance-restricted dynamic cut}
\usage{
ld_prune_and_eMLG(
  GTs,
  stage1,
  ld_w_col,
  ld_w_threshold,
  score_threshold = 0.8,
  min_r2 = 0.2,
  distance_threshold = 5e+05,
  compute_unflagged_eMLG = TRUE,
  min_n_loci_eMLG = 1,
  min_n_loci_flag = Inf,
  cores = 1
)
}
\arguments{
\item{GTs}{Numeric matrix, individuals x markers.}

\item{stage1}{Output of \code{\link[=ld_complexity_reduction]{ld_complexity_reduction()}}.}

\item{ld_w_col}{Name of the ld_w column in \code{stage1$map_snp} used to flag
which clusters need the expensive treatment (a cluster is flagged if
ANY member exceeds \code{ld_w_threshold}).}

\item{ld_w_threshold}{Flagging threshold.}

\item{score_threshold}{Minimum \code{score_eMLG} a candidate merge's result
must clear (see \code{\link[=dynamic_cut_eMLG]{dynamic_cut_eMLG()}}).}

\item{min_r2}{Minimum r2 required directly between the two sides being
merged (see \code{\link[=dynamic_cut_eMLG]{dynamic_cut_eMLG()}}).}

\item{distance_threshold}{Max consecutive-gap in bp allowed within one
physically-contiguous run (see \code{split_by_distance()}). Clusters more
than this apart are never merged, regardless of correlation.}

\item{compute_unflagged_eMLG}{If \code{FALSE}, skip eMLG computation for
unflagged clusters entirely (usually the large majority) -- for
callers who only need the pruned marker set, not eMLGs. Default \code{TRUE}.}

\item{min_n_loci_eMLG}{Skip storing an eMLG for any group (flagged or
unflagged) with fewer than this many raw loci -- the pruned
representative marker is returned regardless, this only trims the
returned \code{eMLG} matrix. Default \code{1} (no filtering).}

\item{min_n_loci_flag}{Additionally flag (send into the distance-
restricted dynamic cut, alongside the \code{ld_w_threshold} criterion) any
Stage-1 cluster with at least this many raw loci, even if its ld_w
never exceeds \code{ld_w_threshold} -- giving already-substantial low-ld_w
clusters a chance to merge with a physically-nearby, correlated
neighbour instead of just passing through unchanged. Default \code{Inf}
(disabled -- flagging is by \code{ld_w_threshold} alone, existing
behaviour). NOTE: this only usefully restricts the flagged set when
\code{ld_w_threshold} is a real, discriminating cutoff -- at
\code{ld_w_threshold = 0}, virtually every cluster may already exceed it
(checked on one real dataset: 99.998\% of markers had a positive local
LD-support value), so \code{min_n_loci_flag} can end up adding nothing on
top of that; the intended use is a real threshold (e.g. 0.025) plus a
\code{min_n_loci_flag} that pulls in the small number of additional
substantial-but-low-ld_w clusters on top of it.}

\item{cores}{Number of cores for the unflagged-cluster eMLG loop (the
large, embarrassingly-parallel one -- flagged clusters and the
distance-restricted dynamic cut are usually far fewer and stay
serial). Uses \code{parallel::mclapply()} (fork-based copy-on-write) when
\code{cores > 1} and not on Windows, so \code{GTs} is NOT duplicated per worker
as long as workers only read it (they do) -- no need to pre-split GTs
into a list yourself; that would cost more memory, not less, since it
forces eager materialization of many separate objects in the parent
before any forking happens. Default \code{1} (serial, with a progress bar;
parallel execution drops the live bar).}
}
\value{
A list: \code{eMLG} (matrix, individuals x groups that have one --
see \code{compute_unflagged_eMLG}/\code{min_n_loci_eMLG}), \code{groups} (data.table:
group_id, Chr, representative, n_loci, score, has_eMLG, members --
always lists EVERY group regardless of eMLG filtering), \code{pruned}
(character vector of representative markers, one per group -- for
LD-pruning use, unaffected by eMLG filtering).
}
\description{
Second-stage clustering on top of \code{\link[=ld_complexity_reduction]{ld_complexity_reduction()}}'s output,
combining what \code{\link[=merge_ld_clusters]{merge_ld_clusters()}} and \code{\link[=dynamic_cut_eMLG]{dynamic_cut_eMLG()}} would
otherwise do as two separate passes into one: Stage-1 clusters flagged by
local LD support (\code{ld_w}) are consolidated via a single distance-
restricted, quality-gated average-linkage dynamic cut (see
\code{dynamic_cut_eMLG.R}'s file header for the linkage comparison and the two
historical bugs it's gated against), producing both a pruned
representative marker set and an eMLG matrix from the same clustering.
Unflagged clusters pass straight through unchanged. See this file's
header comment for the full rationale, including why this is defensible
for LD-pruning itself (not just eMLG summarization) in a young,
low-recombination population, and why merging is restricted to
physically-contiguous runs rather than allowed genome-wide.
}
\examples{
\dontrun{
stage1 <- ld_complexity_reduction(map = map, LD_decay = ld_decay, rho = 0.5)
result <- ld_prune_and_eMLG(
  GTs = GTs, stage1 = stage1, ld_w_col = "ld_w_095",
  ld_w_threshold = 0.2, score_threshold = 0.80, min_r2 = 0.2,
  distance_threshold = 5e5
)
pruned_markers <- result$pruned
eMLG <- result$eMLG
}

}
