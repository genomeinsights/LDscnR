% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/gds_utils.R
\name{create_gds_from_geno}
\alias{create_gds_from_geno}
\title{Create a GDS file from a genotype matrix}
\usage{
create_gds_from_geno(geno, map, gds_path)
}
\arguments{
\item{geno}{Numeric genotype matrix of allele counts (0,1,2) with individuals in rows and SNPs in columns. NA's are allowed.}

\item{map}{Data frame or \code{data.table} with one row per SNP. Must contain
the columns \code{marker}, \code{Chr}, and \code{Pos}.}

\item{gds_path}{File path where the GDS file will be written.}
}
\value{
An open GDS object.
}
\description{
Converts a genotype matrix and accompanying SNP map into a GDS file
compatible with \pkg{SNPRelate}, and returns an open GDS handle.
}
\details{
The genotype matrix is expected in \strong{individuals x SNPs} format.
SNP metadata are taken from \code{map}.

Internally, the genotype matrix is transposed because
\code{SNPRelate::snpgdsCreateGeno()} expects SNPs in rows when
\code{snpfirstdim = TRUE}.

Sample IDs are generated automatically as \code{ind_1}, \code{ind_2}, etc.
}
