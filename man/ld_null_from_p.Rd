% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/ld_null_from_p.R
\name{ld_null_from_p}
\alias{ld_null_from_p}
\title{Build a null bundle from your own observed and permuted p-values}
\usage{
ld_null_from_p(
  p_obs,
  p_perm,
  ld_ws,
  B = NULL,
  alpha = 0.05,
  rho = colnames(ld_ws),
  qstar = seq(0, 0.95, by = 0.05),
  basis = "user-supplied",
  engine = "user-supplied",
  cores = 1L,
  verbose = TRUE
)
}
\arguments{
\item{p_obs}{Numeric vector of observed per-SNP p-values, aligned to the rows
of \code{ld_ws} (i.e. \code{length(p_obs) == nrow(ld_ws)}).}

\item{p_perm}{The surrogate p-values, in any of three forms:
\itemize{
\item a numeric \strong{matrix} with SNPs in rows and surrogates in columns;
\item a \strong{list} of numeric vectors, one per surrogate;
\item a \strong{function} \code{f(b)} returning the p-value vector of surrogate \code{b}, in
which case \code{B} must be given. Use this to stream from disk.
}}

\item{ld_ws}{Local-LD support matrix (SNPs x \code{ld_w} windows; see \code{\link[=ld_cscore]{ld_cscore()}}).}

\item{B}{Number of surrogates. Required only when \code{p_perm} is a function;
otherwise inferred, and checked if supplied.}

\item{alpha, rho, qstar}{C-score parameters, passed to \code{\link[=ld_cscore]{ld_cscore()}}. These must
match between observed and surrogates -- they are applied identically here.}

\item{basis}{A label for how the surrogates were constructed (e.g.
\code{"within-locality permutation"}). Carried through to the printed output and
the gate table; it is documentation, not behaviour.}

\item{engine}{A label for the association engine that produced the p-values
(e.g. \code{"EMMAX"}, \code{"LFMM"}). Also documentation.}

\item{cores}{Number of cores for reducing surrogates to C-scores
(\code{\link[parallel:mclapply]{parallel::mclapply()}}, so >1 is Unix-only). The reduction is the expensive
step; the scan itself is cheap.}

\item{verbose}{Print progress.}
}
\value{
An \code{ld_null} object -- the same class \code{\link[=structured_null]{structured_null()}} returns, so
everything downstream applies: \code{C_obs} (full observed C-vector), \code{C_surr}
(list of \code{B} sparse surrogate C-vectors), \code{universe} (every marker lit up by
the observed or any surrogate), \code{lambda_obs} and \code{lambda_surr} -- the genomic
inflation factor of the observed scan and of each surrogate -- and the
settings. \code{lambda_surr} is recorded here because the C-score reduction
discards the surrogate p-values, so it cannot be recovered afterwards.
}
\description{
The engine-agnostic entry point to the LD-scan pipeline. Where
\code{\link[=structured_null]{structured_null()}} generates its own surrogates by running a fast EMMAX scan
on MVN-resampled phenotypes, this constructor takes p-values you have already
computed -- from EMMAX, LFMM, BayPass, a GLM, anything -- for the observed data
and for \code{B} permuted (or otherwise surrogate) datasets, and reduces them to the
same \code{ld_null} bundle the rest of the pipeline consumes.
}
\details{
This is what makes the null \emph{yours}: the pipeline never sees your phenotype,
your covariates or your association model, so the surrogate construction is
entirely under your control. Permute in whatever way makes your null hypothesis
the one you mean -- within-locality label shuffling, a spatial kernel draw, a
case-control swap -- run your scan on each, and hand the p-values here. The
only requirement is that every surrogate is scanned with the \emph{same} engine and
settings as the observed data, or the comparison is not like-for-like.

Surrogate C-scores are stored \emph{sparsely} (\code{C > 0} only), so \code{B = 200}
whole-genome surrogates cost a few MB, not gigabytes. \code{p_perm} is consumed one
surrogate at a time and reduced immediately, so a \verb{function(b)} accessor lets
you stream surrogates off disk without ever holding the full
markers-by-\code{B} p-value matrix in memory.
}
\seealso{
\code{\link[=ld_gate]{ld_gate()}}, \code{\link[=ld_region_scan]{ld_region_scan()}}, \code{\link[=ld_scan]{ld_scan()}}, \code{\link[=structured_null]{structured_null()}}
}
