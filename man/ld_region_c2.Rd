% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/ld_region_c2.R
\name{ld_region_c2}
\alias{ld_region_c2}
\title{Second-tier C-score -- integrate tau_C and l_min away}
\usage{
ld_region_c2(
  null,
  edges,
  tau_grid = seq(0.02, 0.5, by = 0.02),
  lmin_grid = c(1L, 2L, 3L, 5L, 10L, 15L, 20L),
  fdr = 0.05,
  anchor_tau = 0.05,
  anchor_lmin = 3L,
  verbose = TRUE
)
}
\arguments{
\item{null}{An \code{ld_null} object (\code{\link[=ld_null_from_p]{ld_null_from_p()}} or \code{\link[=structured_null]{structured_null()}}).}

\item{edges}{An \code{\link[=ld_edges]{ld_edges()}} object built over \code{null$universe}.}

\item{tau_grid}{C-score thresholds to sweep (default \code{seq(0.02, 0.5, by = 0.02)}).}

\item{lmin_grid}{Region sizes to sweep (default \code{c(1, 2, 3, 5, 10, 15, 20)}).}

\item{fdr}{BH level defining significance within a cell (default 0.05).}

\item{anchor_tau, anchor_lmin}{The operating point whose regions are annotated
with \code{C^(2)} (defaults 0.05 and 3). The anchor supplies clean, LD-supported
region boundaries; \code{C^(2)} is insensitive to the exact choice, since a real
locus scores high whether its boundaries are drawn here or in a neighbouring
cell.}

\item{verbose}{Print per-\code{tau} progress.}
}
\value{
An \code{ld_region_c2} object: \code{regions} (the anchor regions with a \code{c2}
column, ordered by it), \code{landscape} (per-cell \code{tau}, \code{l_min}, \code{n_obs},
\code{n_sig}), \code{n_usable} (\verb{|U|}), \code{n_cells}, and \code{params}.
}
\description{
Once the null only assigns per-region significance, \code{tau} and \code{l_min} stop
being things to calibrate and become nuisance parameters -- exactly like the
\code{ld_w} window \code{rho} and the stringency \verb{q*} that the per-marker C-score
(\code{\link[=ld_cscore]{ld_cscore()}}) already integrates over. This applies the same remedy one level
up: run \code{\link[=ld_region_scan]{ld_region_scan()}} across a \verb{(tau, l_min)} grid and score each locus by
the fraction of the grid in which it comes out significant.
}
\details{
\deqn{U = \{g \in G : g \text{ yields} \ge 1 \text{ region with } q_R < \alpha_R\}}
\deqn{C^{(2)}_\ell = \frac{1}{|U|} \sum_{g \in U}
      \mathbf{1}[\exists R \ni \ell \text{ with } q_R(g) < \alpha_R]}

Normalising by the \emph{usable} grid \code{U} -- the cells that produce any significant
region at all -- is what defeats cherry-picking. A locus significant only in the
single cell that happens to maximise the region count scores \verb{1/|U|}, near zero,
while one significant across most of the grid scores near 1. No cell is
privileged, so no cell can be selected after the fact.

\code{C^(2)} is a \strong{ranking}, not a second significance test: the grid is not
independent across cells, and no error rate is controlled by it. Its use is to
order loci that \code{q_R} cannot separate -- which is the normal situation, since
with a permutation null every region worth reporting tends to sit at the
p-floor \code{1/(1+B)}.
}
\section{Relation to the per-SNP C-score}{

The consistency principle is Fang et al.'s (2021, \doi{10.1093/molbev/msab144});
see \code{\link[=ld_cscore]{ld_cscore()}} for the attribution. Applying it a second time, to the
region-calling parameters, is what is new here, and it is only possible
because \code{\link[=ld_cscore]{ld_cscore()}} keeps the first-level score per-SNP: regions can then be
re-formed at every \verb{(tau, l_min)} rather than fixed once.
}

\seealso{
\code{\link[=ld_region_scan]{ld_region_scan()}}, \code{\link[=ld_gate]{ld_gate()}}, \code{\link[=ld_scan]{ld_scan()}}
}
