% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/ld_complexity_reduction.R
\name{ld_complexity_reduction}
\alias{ld_complexity_reduction}
\title{Reduce a Marker Set to LD-Independent Representatives}
\usage{
ld_complexity_reduction(
  GTs,
  map,
  LD_decay,
  rho = 0.5,
  cores = 1,
  ld_w_col = NULL,
  ld_w_threshold = NULL
)
}
\arguments{
\item{GTs}{Numeric matrix of genotype dosages, individuals x markers, with
column names matching \code{map$marker}.}

\item{map}{A \code{data.table} with at least \code{Chr} and \code{marker} columns.}

\item{LD_decay}{An \code{ld_decay} object from \code{\link[=compute_LD_decay]{compute_LD_decay()}}, built with
\code{keep_el = TRUE} (or a valid \code{el_data_folder}).}

\item{rho}{Numeric in (0, 1). Decay level used to derive each
chromosome's clustering threshold via \code{\link[=ld_from_rho]{ld_from_rho()}}. Default \code{0.5}.}

\item{cores}{Number of chromosomes to process in parallel.}

\item{ld_w_col, ld_w_threshold}{Optional. If both are supplied, markers
with \code{map[[ld_w_col]] < ld_w_threshold} skip clustering entirely and
pass straight through as their own singleton cluster -- useful when
most markers are already known to carry little local LD support and
clustering all of them anyway would be wasted work.}
}
\value{
A list with:
\describe{
\item{map_snp}{\code{map}, with \code{CL_id}, \code{n_loci}, \code{is_core} (logical), and
\code{median_ld} (this marker's own median r2 to the rest of its cluster,
\code{NA} for singletons) added. \code{CL_id}/\code{n_loci} are named to match what
\code{\link[=make_eMLGs]{make_eMLGs()}} expects from \code{map_cl}.}
\item{clusters}{One row per cluster: \code{Chr}, \code{CL_id}, \code{core_snp},
\code{median_ld} (the core marker's), \code{n_snps}, and \code{members} (a
list-column of every marker in the cluster, including the core).}
\item{pruned}{Character vector of representative marker names --
\code{clusters$core_snp}, for direct use as a pruned marker set.}
}
}
\description{
Clusters markers into local LD blocks and reduces each block to a single
representative ("core") marker. Intended as an LD-pruning step ahead of
kinship/relatedness matrix estimation (a GRM, EMMAX's \code{K}, BayPass's
\code{OMEGA}), or as the clustering step feeding \code{\link[=make_eMLGs]{make_eMLGs()}} -- it returns
\code{CL_id}/\code{n_loci} columns on the same per-marker basis \code{make_eMLGs()}
expects from its \code{map_cl} argument.
}
\details{
The clustering threshold is derived per chromosome from that chromosome's
own fitted LD-decay curve (\code{rho}, via \code{\link[=ld_from_rho]{ld_from_rho()}}) rather than a fixed
r2 cutoff applied genome-wide, so chromosomes with different background LD
levels get an appropriately different absolute threshold. This threshold
is deliberately NOT allowed to vary further within a chromosome (e.g. by
local decay rate): a region with slower local decay -- a centromere or an
inversion -- is exactly where pruning needs to be MORE aggressive, since
those markers are the most redundant; adapting the threshold to expect
high local LD there and tolerate it would defeat the purpose of pruning.

Clustering is two-stage, using \code{LD_decay$by_chr[[ch]]$el} (the same
precomputed edge list \code{\link[=compute_ld_w]{compute_ld_w()}} uses -- built with
\code{keep_el = TRUE}, so no pairwise LD is recomputed here):
\enumerate{
\item Connected components on the sparse thresholded graph (single
linkage; cheap).
\item WITHIN each component, refined to true complete-linkage
sub-clusters via \code{hclust()} on a small dense sub-matrix built from
\code{el}'s already-available r2 values. This second stage matters:
single linkage alone is vulnerable to "chaining" through a mosaic LD
pattern -- marker A uncorrelated with C, but both bridged into one
component via an intermediate B that's correlated with each -- which
would wrongly treat A and C as redundant. Complete linkage requires
every pairwise r2 within a final cluster to clear the threshold, not
just adjacent ones.
}

The representative within each final sub-cluster is the marker with the
highest median r2 to the rest of its cluster -- not the marker most
correlated with the cluster's mean genotype. The mean-genotype approach
is fragile to inconsistent allele-coding direction within a cluster (a
genuinely central marker coded on the opposite reference allele shows a
spuriously negative correlation with the mean and gets passed over for a
noisier marker); r2 is sign-agnostic, so median r2 doesn't have that
problem, and -- since Stage 2 already builds the pairwise r2 sub-matrix
for \code{hclust()} -- costs nothing extra to compute.
}
