% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/ld_manhattan.R
\name{ld_manhattan}
\alias{ld_manhattan}
\title{LD-aware Manhattan plot}
\usage{
ld_manhattan(
  map,
  value,
  value_label = "value",
  regions = NULL,
  group = NULL,
  group_colours = NULL,
  colour = NULL,
  colour_label = NULL,
  qtn = NULL,
  hline = NULL,
  title = NULL,
  pos_unit = 1e+06,
  x_label = "Position (Mbp)",
  point_size = 1.2
)
}
\arguments{
\item{map}{data.frame/data.table with \code{marker}, \code{Chr}, \code{Pos}.}

\item{value}{y-axis value: a named numeric vector (names = markers) or the name
of a numeric column in \code{map}.}

\item{value_label}{y-axis label.}

\item{regions}{Optional list of member-marker vectors (e.g.
\code{ld_outlier_regions()$regions}); each region gets its own palette colour.}

\item{group}{Optional named vector (names = markers) of discrete group labels
(markers absent from it are drawn grey). Takes precedence over \code{regions}; use
it to colour by detection status (\code{"TP"}/\code{"FP"}) etc.}

\item{group_colours}{Optional named colour vector for the \code{group}/\code{regions}
labels; defaults to \code{\link[=default_cluster_colours]{default_cluster_colours()}} recycled.}

\item{colour}{Optional continuous colouring: a named numeric vector (names =
markers) or a numeric column name in \code{map}, drawn with a viridis gradient.
Used only when neither \code{regions} nor \code{group} is given.}

\item{colour_label}{Legend title for the continuous \code{colour}.}

\item{qtn}{Optional character vector of markers to mark with black crosses.}

\item{hline}{Optional y value for a dashed horizontal reference line
(e.g. \code{tau_C}, or \code{-log10(0.05)}).}

\item{title}{Plot title.}

\item{pos_unit}{Divide \code{Pos} by this for the x-axis (default \code{1e6} -> Mb).}

\item{x_label}{x-axis label (default \code{"Position (Mbp)"}).}

\item{point_size}{Size of the coloured (grouped / continuous) points.}
}
\value{
A \code{ggplot} object.
}
\description{
A per-chromosome (faceted) Manhattan plot for outlier-region analyses. The
y-axis is any per-marker value -- a C-score, \code{-log10(q)}, an \code{ld_w} window, an
association statistic. Points are coloured in one of two modes: \strong{discrete} --
by outlier region (or any per-marker group label, e.g. TP/FP), each group its
own colour and ungrouped markers grey; or \strong{continuous} -- by a numeric
per-marker column via a viridis gradient. Highlight markers (e.g. true QTNs) can
be marked with crosses, and a dashed threshold line drawn.
}
\details{
Coloring precedence: \code{group} > \code{regions} > \code{colour}. Supply exactly one.
}
\seealso{
\code{\link[=ld_outlier_regions]{ld_outlier_regions()}}, \code{\link[=ld_cscore]{ld_cscore()}}, \code{\link[=default_cluster_colours]{default_cluster_colours()}}
}
