% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/structured_null.R
\name{null_fdr}
\alias{null_fdr}
\title{Region-level FDR of the C-score from a structure-aware null}
\usage{
null_fdr(bundle, edges, tau_grid = seq(0.05, 1, by = 0.05), l_min = 2L)
}
\arguments{
\item{bundle}{An \code{ld_null} object from \code{\link[=structured_null]{structured_null()}}.}

\item{edges}{An \code{\link[=ld_edges]{ld_edges()}} object built over \code{bundle$universe}.}

\item{tau_grid}{C-score thresholds to evaluate (default \code{seq(0.05, 1, 0.05)}).}

\item{l_min}{Minimum region size in SNPs (default 2).}
}
\value{
A data.frame with \code{tau_C}, \code{n_obs}, \code{n_null}, \code{fdr}.
}
\description{
Turns an \code{\link[=structured_null]{structured_null()}} bundle into an empirical false-discovery-rate
curve over \code{tau_C}, at the region unit: for each \code{tau_C}, cluster the gated
SNPs (\code{\link[=ld_regions]{ld_regions()}}) and count regions of at least \code{l_min} SNPs, both for the
observed C and (averaged) for the surrogates. \verb{FDR = mean surrogate regions / observed regions}. Because structure-only surrogates almost never produce large
clusters, a large \code{l_min} makes the null very clean -- the region-size filter
is itself a strong false-positive control.
}
\seealso{
\code{\link[=structured_null]{structured_null()}}, \code{\link[=calibrate_tauc]{calibrate_tauc()}}, \code{\link[=ld_edges]{ld_edges()}}
}
