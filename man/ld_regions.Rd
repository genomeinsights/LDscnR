% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/ld_regions.R
\name{ld_regions}
\alias{ld_regions}
\title{Cluster gated markers into outlier regions}
\usage{
ld_regions(markers, edges)
}
\arguments{
\item{markers}{Character vector of gated markers, a subset of the \code{\link[=ld_edges]{ld_edges()}}
universe, e.g. \code{names(C)[C >= tau_C]} for a C-score \code{C} (\code{\link[=ld_cscore]{ld_cscore()}}).}

\item{edges}{An \code{ld_edges} object from \code{\link[=ld_edges]{ld_edges()}}.}
}
\value{
A list of character vectors, one per outlier region (its member
markers). Apply the region-size filter with
\code{regions[lengths(regions) >= l_min]}.
}
\description{
Partitions a gated marker set into outlier regions by single-linkage on the
cached r^2-edge graph (\code{\link[=ld_edges]{ld_edges()}}) -- i.e. connected components of the
induced r^2 subgraph -- followed by a physical gap-split wherever consecutive
members are more than the chromosome's distance cap apart. Needs no genotype
access, so any \code{tau_C} / \code{l_min} can be scored ad hoc; reproduces the
clustering step of \code{\link[=ld_outlier_clusters]{ld_outlier_clusters()}} exactly.
}
\seealso{
\code{\link[=ld_edges]{ld_edges()}}, \code{\link[=ld_cscore]{ld_cscore()}}
}
