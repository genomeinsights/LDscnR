% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/ld_region_stability.R
\name{ld_region_stability}
\alias{ld_region_stability}
\alias{ld_region_stability.ld_outlier_regions}
\alias{ld_region_stability.default}
\title{Rank outlier regions by cross-parameter stability (threshold-free)}
\usage{
ld_region_stability(x, ...)

\method{ld_region_stability}{ld_outlier_regions}(x, tau_grid = "auto", l_min_grid = "auto", map = NULL, ...)

\method{ld_region_stability}{default}(
  x,
  edges,
  regions = NULL,
  null = NULL,
  tau_grid = "auto",
  l_min_grid = "auto",
  base_tau = NULL,
  base_lmin = 2L,
  fdr = 0.05,
  max_tau = 40L,
  lmin_cap = 20L,
  map = NULL,
  ...
)
}
\arguments{
\item{x}{Either a named per-SNP C-score vector (\code{\link[=ld_cscore]{ld_cscore()}} /
\code{\link[=ld_cscore_scan]{ld_cscore_scan()}}), or an \code{ld_outlier_regions} object built with
\code{keep_cache = TRUE} (its \code{C}, \code{edges}, \code{regions}, \code{l_min}, and \code{null} are used).}

\item{...}{Passed on from the generic to the default method.}

\item{tau_grid, l_min_grid}{Grid axes, or \code{"auto"} (default) for the data-driven
grid described above.}

\item{map}{Optional \code{marker}/\code{Chr}/\code{Pos} table to annotate each region with
\code{Chr}, \code{start}, \code{end}.}

\item{edges}{An \code{\link[=ld_edges]{ld_edges()}} object over the full \code{C > 0} universe (from
\code{\link[=ld_cscore_scan]{ld_cscore_scan()}} or \code{\link[=ld_edges]{ld_edges()}}). Required for the vector method.}

\item{regions}{Optional list of base regions (member-marker vectors) to rank.
If \code{NULL}, regions are clustered at \code{base_tau} and filtered to \verb{>= base_lmin}.}

\item{null}{Optional \code{\link[=structured_null]{structured_null()}} bundle (needs \code{C_surr} over the same
\code{edges} universe); enables the null-aware \code{stability_null} value.}

\item{base_tau, base_lmin}{The \code{tau_C} / \code{l_min} defining the base region set
when \code{regions} is \code{NULL}, and the \code{l_min} reference for \code{persist_tau}
(defaults: \code{min(tau_grid)}, \code{2}).}

\item{fdr}{Cell is null-clean when its region-level FDR (mean surrogate regions
/ observed regions) is at or below this (default \code{0.05}).}

\item{max_tau, lmin_cap}{Caps on the auto grid's axis lengths (defaults 40, 20).}
}
\value{
An \code{ld_region_stability} data.table, ordered most- to least-stable,
with \code{region}, (\code{Chr}, \code{start}, \code{end} if \code{map}), \code{size}, \code{maxC},
\code{persist_tau}, \code{n_cells}, \code{stability}, \code{n_clean}, \code{stability_null},
\code{rank_stability}, \code{rank_persist}. The grid, cell total, and whether a null was
used are stored in the \code{"grid"} attribute.
}
\description{
Ranks LD-aware outlier regions without committing to any single \code{tau_C} or
\code{l_min}, by asking \emph{in how many} of a \code{tau_C} x \code{l_min} grid of cells each
region survives -- the C-score idea lifted from the SNP to the region level.
It sets no threshold; it only orders regions, so it sidesteps the arbitrary
cutoff a \code{maxC}-style filter would impose.
}
\details{
For every \code{tau_C} on the grid the lit-up SNPs are re-clustered (\code{\link[=ld_regions]{ld_regions()}})
and each base region is mapped to the largest re-clustered region covering any
of its members (its \code{maxsz} at that \code{tau_C}); the region \emph{survives} a cell
\verb{(tau_C, l_min)} when \code{maxsz >= l_min}. Two complementary statistics fall out:
\itemize{
\item \strong{\code{stability}} = the fraction of the grid's cells in which the region
survives (\verb{n_cells / (|tau_grid| * |l_min_grid|)}) -- a threshold-free
region-level consistency score; rank regions by it (or by the raw
\code{n_cells}). Large, consistently-elevated blocks survive nearly every cell;
single high-C spikes survive only the low-\code{l_min} cells.
\item \strong{\code{persist_tau}} = the largest \code{tau_C} on the grid at which the region
still holds \verb{>= base_lmin} consistent SNPs (a continuous strength).
}

\strong{Adaptive grid (default \code{tau_grid = "auto"}, \code{l_min_grid = "auto"}).} The
grid covers exactly where regions are produced rather than a fixed box: the
\code{tau_C} axis is the distinct observed C values (data-driven breakpoints, from
the most permissive up to \code{maxC} where the last region vanishes, thinned to
\code{max_tau}), and the \code{l_min} axis runs from 1 to the largest region at the most
permissive \code{tau_C} (capped at \code{lmin_cap}). Supply explicit vectors to override.

\strong{Null-aware value (supply \code{null}).} Given a structure-aware null bundle
(\code{\link[=structured_null]{structured_null()}}), a cell \verb{(tau_C, l_min)} is \emph{clean} when its region-level
FDR -- mean surrogate regions divided by observed regions -- is at or below
\code{fdr}. (A strict "no surrogate region at all" rule is degenerate at \code{B >= 100},
where the mean count is essentially never exactly zero; the FDR gate is its
robust form.) The region then scores \strong{\code{stability_null}} = the number of cells
where it is found \emph{and} the cell is clean, divided by \emph{all} grid cells. Keeping
the denominator at all cells means that when the null lights up much of the
parameter space the value drops for every region -- automatically discounting
datasets (or regions) whose apparent consistency lives where the null is also
active. Ranking uses \code{stability_null} when a null is supplied, and the plain
grid-survival \code{stability} otherwise.
}
\seealso{
\code{\link[=ld_cscore_scan]{ld_cscore_scan()}}, \code{\link[=ld_regions]{ld_regions()}}, \code{\link[=ld_outlier_regions]{ld_outlier_regions()}}, \code{\link[=structured_null]{structured_null()}}
}
