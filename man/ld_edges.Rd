% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/ld_regions.R
\name{ld_edges}
\alias{ld_edges}
\title{Pre-compute the LD r^2-edge graph over a candidate marker set}
\usage{
ld_edges(
  markers,
  GTs,
  map,
  decay_sum,
  rho_ld = 0.9,
  dcap = 5e+05,
  r2_link = NULL,
  rho_d = NULL
)
}
\arguments{
\item{markers}{Character vector of candidate markers (the "universe" that any
downstream \code{tau_C}/\code{l_min} gate is a subset of).}

\item{GTs}{Genotype dosage matrix (individuals x SNPs; column names = markers).}

\item{map}{data.frame/data.table with columns \code{marker}, \code{Chr}, \code{Pos}.}

\item{decay_sum}{Per-chromosome LD-decay summary with \code{Chr}, \code{b}, \code{c},
\code{a_pred} (see \code{\link[=compute_LD_decay]{compute_LD_decay()}}).}

\item{rho_ld}{Decay-relative r^2 link threshold (default 0.9).}

\item{dcap}{Hard distance cap in bp for the gap-split (default 5e5).}

\item{r2_link}{Optional scalar r^2 overriding the decay-relative value.}

\item{rho_d}{Optional decay-relative distance; overrides the hard cap but is
itself capped at \code{dcap}.}
}
\value{
An object of class \code{ld_edges}: a per-chromosome list of member
markers, positions, the chromosome's r^2 / distance thresholds, and the r^2
edge list (a two-column character matrix of linked marker pairs). Consumed
by \code{\link[=ld_regions]{ld_regions()}}.
}
\description{
Builds the within-chromosome r^2 linkage graph among a set of candidate
markers so that clustering any gated subset later (\code{\link[=ld_regions]{ld_regions()}}) is a pure
graph lookup, with no further genotype access. Two markers are linked when
their r^2 exceeds a (by default LD-decay-informed) threshold \code{r2_link}.
}
\details{
The r^2 threshold is decay-relative and self-calibrates per chromosome:
\code{r2_link = ld_from_rho(b, c, rho_ld)}. \code{rho_ld = 0.9} is the canonical value
(shown insensitive on simulated and empirical data). The physical extent of an
outlier region is bounded by a hard distance cap \code{dcap} (default 500 kb)
rather than by LD decay, because a region's footprint is set by
selection/recombination, not by background LD decay -- long-range r^2 links are
the rule inside inversions, so a decay-derived distance would fragment them.
Supply a scalar \code{r2_link} to override the decay-relative r^2, or \code{rho_d} to
derive the distance from LD decay (capped at \code{dcap}) instead of the hard cap.
}
\seealso{
\code{\link[=ld_regions]{ld_regions()}}
}
