% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/ld_outlier_regions.R
\name{ld_outlier_regions}
\alias{ld_outlier_regions}
\title{Call LD-aware outlier regions with a null-calibrated threshold}
\usage{
ld_outlier_regions(
  y,
  GTs,
  K,
  ld_ws,
  map,
  decay_sum,
  alpha = 0.05,
  rho_ld = 0.75,
  dcap = 5e+05,
  rho_d = NULL,
  l_min = 2L,
  lmin_q = 0.99,
  lmin_tau = 0.05,
  fdr = 0.05,
  basis = c("genetic", "spatial"),
  coords = NULL,
  B = 100L,
  rho = colnames(ld_ws),
  qstar = seq(0, 0.95, by = 0.05),
  tau_grid = seq(0.05, 1, by = 0.05),
  keep_cache = FALSE,
  seed = 1L
)
}
\arguments{
\item{y}{Phenotype vector (length = individuals).}

\item{GTs}{Genotype dosage matrix (individuals x SNPs; column names = markers).}

\item{K}{Kinship / GRM (see \code{\link[=structured_null]{structured_null()}} on the recommended GRM).}

\item{ld_ws}{Local-LD support matrix (SNPs x \code{ld_w} windows; see \code{\link[=ld_cscore]{ld_cscore()}}).}

\item{map}{data.frame/data.table with \code{marker}, \code{Chr}, \code{Pos} (aligned to \code{GTs}).}

\item{decay_sum}{Per-chromosome LD-decay summary (see \code{\link[=ld_edges]{ld_edges()}}).}

\item{alpha}{Fixed within-candidate FDR for the C-score (default 0.05).}

\item{rho_ld, dcap, rho_d}{Clustering parameters (see \code{\link[=ld_edges]{ld_edges()}}; defaults 0.9,
500 kb hard cap, \code{rho_d = NULL}).}

\item{l_min}{Minimum region size in SNPs (default 2), or \code{"auto"} to set it
data-drivenly from the structure null via \code{\link[=calibrate_lmin]{calibrate_lmin()}}.}

\item{lmin_q, lmin_tau}{Passed to \code{\link[=calibrate_lmin]{calibrate_lmin()}} when \code{l_min = "auto"}: the
upper quantile of the null max-region-size distribution (default 0.99) and the
reference C-threshold at which it is read (default 0.05).}

\item{fdr}{Target region-level FDR for \code{tau_C} (default 0.05).}

\item{basis, coords}{Structure basis for the null (see \code{\link[=structured_null]{structured_null()}}).}

\item{B}{Number of null surrogates (default 100).}

\item{rho, qstar}{C-score integration grids (see \code{\link[=ld_cscore]{ld_cscore()}}).}

\item{tau_grid}{\code{tau_C} grid searched for the FDR target.}

\item{keep_cache}{If \code{TRUE}, also return the null bundle and edge cache for
ad-hoc re-estimation at other \code{tau_C} / \code{l_min}.}

\item{seed}{RNG seed for the null.}
}
\value{
An \code{ld_outlier_regions} object: \code{regions} (list of member-marker
vectors), \code{summary} (per-region \code{Chr}, \code{start}, \code{end}, \code{size}, \code{maxC}),
\code{tau_C}, \code{C} (full per-SNP C-score), \code{fdr_curve}, \code{params}, and -- if
\code{keep_cache} -- \code{null} and \code{edges}.
}
\description{
The end-to-end LD-scan pipeline. From a phenotype, genotypes and a kinship it
(1) runs a fast EMMAX scan, (2) computes the per-SNP consistency C-score
(\code{\link[=ld_cscore]{ld_cscore()}}) integrating the \code{ld_w} window and stringency, (3) draws a
structure-aware null (\code{\link[=structured_null]{structured_null()}}) and calibrates the C-score threshold
\code{tau_C} to a target FDR at the region unit (\code{\link[=calibrate_tauc]{calibrate_tauc()}}), and (4)
clusters the SNPs above \code{tau_C} into outlier regions (\code{\link[=ld_regions]{ld_regions()}}) keeping
those with at least \code{l_min} SNPs.
}
\details{
Observed statistics and the null share one engine (fast EMMAX with \code{K}), which
is essential for a valid calibration: the null must be able to reproduce the
observed structure-driven false positives. For a method without a fast null
(e.g. LFMM), compute its C-score separately and transfer this run's \code{tau_C}
with \code{\link[=gc_map_tauc]{gc_map_tauc()}}.

The two swept parameters (\code{rho}, \verb{q*}) are integrated into the C-score; \code{alpha}
is fixed; clustering is LD-decay-informed (\code{rho_ld}) with a hard distance cap
(\code{dcap}). The two operational knobs are \code{tau_C} (calibrated here, not set) and
\code{l_min} (region size; the region-level null makes a larger \code{l_min} a strong,
clean false-positive control).
}
\section{Choosing between this and ld_scan()}{

This wrapper runs its own EMMAX scan and generates its own MVN surrogates, so
it is tied to that one engine and to a null it constructs for you. If you have
p-values already -- from any engine -- and want to define the surrogates
yourself, use \code{\link[=ld_scan]{ld_scan()}} instead: it takes observed and permuted p-values,
applies the location-matched region test, and ranks the result with
\code{\link[=ld_region_c2]{ld_region_c2()}} rather than calibrating a single \code{tau_C}.
}

\seealso{
\code{\link[=ld_scan]{ld_scan()}} (engine-agnostic entry point), \code{\link[=ld_cscore]{ld_cscore()}},
\code{\link[=structured_null]{structured_null()}}, \code{\link[=ld_regions]{ld_regions()}}, \code{\link[=gc_map_tauc]{gc_map_tauc()}}, \code{\link[=evaluate_ors]{evaluate_ors()}}
}
