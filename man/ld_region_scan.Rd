% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/ld_region_scan.R
\name{ld_region_scan}
\alias{ld_region_scan}
\title{Call outlier regions and test each against the null at its own locus}
\usage{
ld_region_scan(null, edges, tau = 0.05, l_min = 3L, fdr = 0.05)
}
\arguments{
\item{null}{An \code{ld_null} object (\code{\link[=ld_null_from_p]{ld_null_from_p()}} or \code{\link[=structured_null]{structured_null()}}).}

\item{edges}{An \code{\link[=ld_edges]{ld_edges()}} object built over \code{null$universe}.}

\item{tau}{C-score threshold for forming regions (default 0.05).}

\item{l_min}{Minimum region size in markers (default 3).}

\item{fdr}{BH level at which \code{sig} is flagged (default 0.05).}
}
\value{
An \code{ld_region_scan} object: \code{regions} (a \code{data.table} with \code{chr},
\code{lo}, \code{hi}, \code{size}, \code{s_R}, \code{n_null_ge}, \code{p_R}, \code{q_R}, \code{sig}), the \code{p_floor}
\code{1/(1+B)}, and \code{params}.
}
\description{
The one instrument of the LD-scan pipeline. Regions are formed by clustering
the markers whose C-score reaches \code{tau} (\code{\link[=ld_regions]{ld_regions()}}) and keeping those with
at least \code{l_min} markers. Each observed region \code{R} is scored by its summed
C-mass, and its p-value is the fraction of surrogates that produce a region
\emph{overlapping R's own genomic span} with at least as much mass:
}
\details{
\deqn{s_R = \sum_{j \in R} C_j, \qquad
      p_R = \frac{1 + \#\{b : s^{null}_{b,R} \ge s_R\}}{1 + B}}

followed by Benjamini-Hochberg across the discovered regions to give \code{q_R}.

The test is deliberately \strong{location-matched}, and that is what makes it work
where a genome-wide count does not. A signal confined to part of the sampled
range need not be reproduced by a structure-preserving surrogate \emph{at its own
locus}, so a genuinely range-restricted peak -- which a pooled count would
charge to structure -- can still separate. It is also robust to a globally
inflated null, because excess null peaks are scattered and rarely land on any
particular observed locus.

\code{q_R}, not a Bonferroni correction over every marker, is the multiple-testing
correction this pipeline reports: the per-marker burden was already carried at
the C-score and region-size stages.

\strong{Run \code{\link[=ld_gate]{ld_gate()}} first.} A p-value from a null whose surrogates reproduce the
observed background is not interpretable, and nothing in this function can
detect that for you.
}
\seealso{
\code{\link[=ld_gate]{ld_gate()}}, \code{\link[=ld_region_c2]{ld_region_c2()}}, \code{\link[=ld_scan]{ld_scan()}}, \code{\link[=ld_null_from_p]{ld_null_from_p()}}
}
