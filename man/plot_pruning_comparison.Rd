% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/plot_pruning_comparison.R
\name{plot_pruning_comparison}
\alias{plot_pruning_comparison}
\title{Plot Stage 1 vs combined (ld_prune_and_eMLG) cluster diagnostic for one chromosome}
\usage{
plot_pruning_comparison(
  chr,
  pruned_stage1,
  result,
  map,
  ld_w_col = "ld_w_095",
  ld_w_threshold = 0.2,
  min_n_loci_flag = Inf,
  direction = c("high", "low"),
  out_folder = "./Figures/",
  width = 10,
  height = 9
)
}
\arguments{
\item{chr}{Chromosome name (e.g. "Chr26"), used in plot labels/filename.}

\item{pruned_stage1}{Output of \code{\link[=ld_complexity_reduction]{ld_complexity_reduction()}} (whole-genome).}

\item{result}{Output of \code{\link[=ld_prune_and_eMLG]{ld_prune_and_eMLG()}} (whole-genome).}

\item{map}{Map data.table with Chr, Pos, marker, and \code{ld_w_col}.}

\item{ld_w_col}{Name of the ld_w column in \code{map}/\code{pruned_stage1$map_snp}.}

\item{ld_w_threshold}{Threshold used to flag clusters for the Stage-1
comparison view -- should match what was used to build \code{result}.}

\item{min_n_loci_flag}{Must match whatever \code{min_n_loci_flag} was passed
to the \code{\link[=ld_prune_and_eMLG]{ld_prune_and_eMLG()}} call that produced \code{result} (default
\code{Inf}, i.e. off). Without this, the Stage 1 panel (pure \code{ld_w_col > ld_w_threshold}) and the Combined panel (\code{result$groups}' "F"/"U"
prefixes, which also reflect \code{min_n_loci_flag}) fall out of sync:
clusters pulled into the flagged/merge pathway purely by size, not
ld_w, show up in the Combined "high" panel and vanish from Combined
"low" with no Stage-1-side counterpart -- checked directly on real
data (ld_w_threshold=0.025, min_n_loci_flag=5): ~1.5\% of markers landed
in Combined "F" this way, missing from Combined "U", before this
parameter was added.}

\item{direction}{"high" (default) shows the FLAGGED clusters/groups
(\code{ld_w_col > threshold} OR \code{n_snps >= min_n_loci_flag}, group_id prefix
"F") -- the main diagnostic view. "low" shows the UNFLAGGED side (the
complement, group_id prefix "U") as a sanity check: this view should
never contain markers with \code{ld_w_col > threshold}, since "unflagged" is
defined as no cluster member exceeding threshold AND the cluster not
being pulled in by \code{min_n_loci_flag}. The "high" view, by contrast, is
expected to contain some sub-threshold "boundary" markers (cluster-
mates of a flagged marker, or whole clusters flagged only via
\code{min_n_loci_flag}) since flagging happens at the cluster level, not the
marker level.}

\item{out_folder}{Folder to write the figure to (created if missing).}

\item{width, height}{Figure dimensions in inches, passed to
\code{ggplot2::ggsave()}.}
}
\value{
The stacked comparison plot, invisibly (also saved to disk as
\verb{<out_folder>/<chr>_stage1_vs_combined_<direction>.png}).
}
\description{
A diagnostic scatter plot (\code{ld_w} vs. position, points coloured by
cluster) stacking \code{\link[=ld_complexity_reduction]{ld_complexity_reduction()}}'s raw Stage-1 clusters on
top of \code{\link[=ld_prune_and_eMLG]{ld_prune_and_eMLG()}}'s consolidated groups for one chromosome, so
fragmented low-recombination blocks (Stage 1, top) and their reunited
counterparts (Combined, bottom) can be compared directly. Saves the
stacked figure to disk and returns it invisibly.
}
\examples{
\dontrun{
stage1 <- ld_complexity_reduction(map = map, LD_decay = ld_decay, rho = 0.5)
result <- ld_prune_and_eMLG(
  GTs = GTs, stage1 = stage1, ld_w_col = "ld_w_095", ld_w_threshold = 0.2
)
plot_pruning_comparison("Chr1", stage1, result, map, ld_w_threshold = 0.2)
}

}
