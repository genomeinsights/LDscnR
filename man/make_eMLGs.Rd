% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/eMLG_complexity_reduction.R
\name{make_eMLGs}
\alias{make_eMLGs}
\title{Build eMLGs (Expected Multi-Locus Genotypes) From Pre-Defined Clusters}
\usage{
make_eMLGs(
  GTs,
  map_cl,
  input = c("auto", "hard", "dosage"),
  cor_th = 0.8,
  l_min = 10,
  ncores = 1,
  col_vector = NULL
)
}
\arguments{
\item{GTs}{Numeric matrix of genotypes, individuals x markers (hard-called
0/1/2 or continuous dosage -- see \code{input}).}

\item{map_cl}{A data.table/data.frame with at least \code{marker}, \code{CL_id}, and
\code{n_loci} columns (rows with \code{NA} \code{CL_id} are dropped).}

\item{input}{\code{"auto"} (default) detects hard-called vs. dosage genotypes
from the data itself; \code{"hard"}/\code{"dosage"} force one or the other.}

\item{cor_th}{Stored on the returned object for downstream reference; not
used to filter clusters here (compare \code{\link[=dynamic_cut_eMLG]{dynamic_cut_eMLG()}}'s
\code{threshold}, which does gate on it).}

\item{l_min}{Minimum cluster size (raw marker count) to keep; smaller
clusters are dropped entirely.}

\item{ncores}{Number of cores for the per-cluster consensus computation
(\code{parallel::mclapply()}, fork-based -- not available on Windows).}

\item{col_vector}{Optional vector of colours to cycle across clusters for
plotting; defaults to \code{\link[=default_cluster_colours]{default_cluster_colours()}}.}
}
\value{
A list: \code{eMLG} (matrix, individuals x clusters), \code{map_eMLG}
(one row per cluster: position span, \code{n_loci}, \code{r2_eMLG}), \code{map_SNP}
(one row per marker, with its cluster's \code{r2_eMLG}/colour attached),
\code{clusters} (named list of marker vectors, one per cluster), \code{input},
\code{cor_th}.
}
\description{
Reduces a pre-existing set of LD clusters (e.g. from \code{\link[=ld_complexity_reduction]{ld_complexity_reduction()}}
or any other clustering that assigns a \code{CL_id} per marker) directly to one
consensus genotype per cluster -- no merging or restructuring of the
cluster boundaries themselves. For that, see \code{\link[=dynamic_cut_eMLG]{dynamic_cut_eMLG()}} /
\code{\link[=ld_prune_and_eMLG]{ld_prune_and_eMLG()}}, which additionally consolidate clusters that are
still correlated with each other via a quality-gated dynamic tree cut;
\code{make_eMLGs()} is the simpler building block that just summarizes clusters
as they already stand.
}
\examples{
\dontrun{
eMLGs <- make_eMLGs(
  GTs, map_cl[!is.na(CL_id) & n_loci >= 10, ],
  cor_th = 0.8, l_min = 1, ncores = 4
)
}

}
