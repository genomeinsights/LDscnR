% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/ld_benchmark.R
\name{qtn_ld_table}
\alias{qtn_ld_table}
\title{LD (r^2) and distance between candidate markers and QTNs}
\usage{
qtn_ld_table(GTs, map, candidate_markers, max_bp = 2e+06, cores = 1)
}
\arguments{
\item{GTs}{Genotype dosage matrix (individuals x SNPs; column names = markers).}

\item{map}{data.table with \code{marker}, \code{Chr}, \code{Pos}, \code{type}.}

\item{candidate_markers}{Markers to tabulate (the OR-membership universe).}

\item{max_bp}{Maximum candidate-QTN distance to retain (default 2e6).}

\item{cores}{Cores for the per-chromosome loop.}
}
\value{
A data.table with \code{marker}, \code{qtn_marker}, \code{r2}, \code{dist_bp}.
}
\description{
For every candidate marker, its r^2 and physical distance to each QTN on the
same chromosome within \code{max_bp}. This is the lookup that decides whether an
outlier region is "close enough" to a QTN to count as a detection
(\code{\link[=classify_ors]{classify_ors()}}, \code{\link[=evaluate_ors]{evaluate_ors()}}).
}
