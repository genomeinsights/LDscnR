% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/ld_outlier_perm.R
\name{ld_outlier_perm}
\alias{ld_outlier_perm}
\title{Permutation null for \code{ld_outlier_test()}: how many discoveries under no signal}
\usage{
ld_outlier_perm(
  obs,
  stage1,
  map,
  p_perm,
  GTs = NULL,
  LD_decay = NULL,
  B = NULL,
  level = c("units", "regions"),
  cores = 1L,
  verbose = TRUE
)
}
\arguments{
\item{obs}{An \code{ld_outlier_test} result -- the observed call, whose
\verb{$params} are reused for every surrogate so the null is calibrated under
exactly the same \code{statistic}/\code{size_floor}/\code{alpha}/\code{assembly} as the
observed count.}

\item{stage1, map, GTs, LD_decay}{Same as \code{\link[=ld_outlier_test]{ld_outlier_test()}}; only \code{GTs}/
\code{LD_decay} are actually needed here for \code{assembly = "stage2_discovered"},
passed through unchanged for every surrogate.}

\item{p_perm}{Surrogate p-values, same shape \code{p_obs} had in \code{obs}: a
markers/units-by-\code{B} matrix, a list of vectors, or a \verb{function(b)}.}

\item{B}{Number of surrogates; required only if \code{p_perm} is a function.}

\item{level}{\code{"units"} (default; count significant units per surrogate) or
\code{"regions"} (count assembled regions per surrogate) -- the two need not
agree, since assembly can merge several significant units into one region.}

\item{cores}{Cores for the surrogate loop (Unix only).}

\item{verbose}{Print progress (default \code{TRUE}).}
}
\value{
An \code{ld_outlier_perm} object: \code{observed} (the count from \code{obs},
at \code{level}), \code{surrogates} (integer vector, length \code{B}), \code{p} (one-sided,
\verb{(1 + #surrogates >= observed) / (B + 1)}), \code{realised_fdr}
(\code{mean(surrogates) / observed}), \code{params}.
}
\description{
Not separate machinery. This runs the SAME \code{\link[=ld_outlier_test]{ld_outlier_test()}} pipeline once
per column of \code{p_perm} and reports the surrogate distribution of discovery
counts against the observed one -- the null falls out of code that already
exists rather than being reimplemented for validation.
}
\details{
What "no signal" means is entirely determined by how \code{p_perm}'s surrogates
were generated, and that is the caller's design, not this function's: which
unit is permuted, what structure is held fixed. A scheme that breaks the
structure your association model corrects for produces an anticonservative
null that no amount of downstream care repairs -- this function cannot detect
that and does not try to.
}
\seealso{
\code{\link[=ld_outlier_test]{ld_outlier_test()}}, \code{\link[=ld_region_rotation]{ld_region_rotation()}}
}
