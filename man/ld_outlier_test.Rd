% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/ld_outlier_test.R
\name{ld_outlier_test}
\alias{ld_outlier_test}
\title{Test LD-complexity-reduction clusters and assemble significant ones into regions}
\usage{
ld_outlier_test(
  stage1,
  map,
  p_obs,
  statistic = c("simes", "unit"),
  size_floor = 8L,
  alpha = 0.05,
  assembly = c("stage2_discovered", "physical"),
  GTs = NULL,
  LD_decay = NULL,
  score_threshold = 0.8,
  distance_threshold = 1e+05,
  gap = 3e+05
)
}
\arguments{
\item{stage1}{An \code{ld_complexity_reduction} object (must have \verb{$params$rho}).}

\item{map}{data.frame/data.table with \code{marker}, \code{Chr}, \code{Pos}, aligned to
\code{stage1}'s marker universe.}

\item{p_obs}{Per-marker p-values (if \code{statistic = "simes"}), aligned to
\code{map$marker}, or per-unit p-values in unit order (if \code{statistic = "unit"},
aligned to the units \code{\link[=ld_unit_matrix]{ld_unit_matrix()}} would return at the same
\code{size_floor}).}

\item{statistic}{\code{"simes"} or \code{"unit"}.}

\item{size_floor}{Minimum markers per tested unit (default 8L).}

\item{alpha}{BH level (default 0.05).}

\item{assembly}{\code{"stage2_discovered"} (default) -- re-run
\code{\link[=ld_prune_and_eMLG]{ld_prune_and_eMLG()}} over the significant clusters only, so a region can
only span discovered signal -- or \code{"physical"}, a plain merge of
significant clusters within \code{gap} on a chromosome. The two are not
equivalent in general; \code{"physical"} is retained as a near-free check
(agreement between an arbitrary rule and a motivated one is evidence
neither is an artefact of the other), not as an equally-preferred
alternative.}

\item{GTs, LD_decay}{Required only when \code{assembly = "stage2_discovered"}.}

\item{score_threshold, distance_threshold}{\code{\link[=ld_prune_and_eMLG]{ld_prune_and_eMLG()}} parameters
for \code{"stage2_discovered"}. \code{ld_w_threshold} is fixed at 0 internally, not
exposed: used only to merge already-significant clusters it is not a
gate, only a speed filter, and 0 filters nothing.}

\item{gap}{Physical merge distance in bp, used only when
\code{assembly = "physical"}.}
}
\value{
An \code{ld_outlier_test} object: \code{units} (data.table, one row per tested
unit -- \code{unit_id}, \code{Chr}, \code{from}, \code{to}, \code{n_markers}, \code{p}, \code{q},
\code{significant}), \code{regions} (data.table, one row per assembled region --
\code{Chr}, \code{from}, \code{to}, \code{n_units}, \code{n_markers}, \code{occupancy}), \code{params}
(everything above, resolved).
}
\description{
The core detection step of the stage-1-cluster outlier-region method: one of
two genuinely different algorithms in this package for turning per-marker
p-values plus LD structure into significant regions (the other is the
consistency C-score family behind \code{\link[=ld_scan]{ld_scan()}}/\code{\link[=ld_outlier_regions]{ld_outlier_regions()}}). No
null here -- see \code{\link[=ld_outlier_perm]{ld_outlier_perm()}} for the permutation null and
\code{\link[=ld_region_rotation]{ld_region_rotation()}} for annotation-overlap validation, both of which
reuse this function's result rather than duplicate its logic.
}
\details{
\code{statistic = "simes"} aggregates marker-level \code{p_obs} (\code{min(n * p_(i) / i)}
per cluster, so the penalty scales with cluster size); \code{statistic = "unit"}
takes \code{p_obs} already computed per unit -- built with \code{\link[=ld_unit_matrix]{ld_unit_matrix()}} and
scanned with your own engine -- and does no aggregation, only BH and assembly.

\code{min_r2_rho} for \code{assembly = "stage2_discovered"} is read from
\code{stage1$params$rho}, NOT a separate argument: the whole point of this
assembly rule is that it introduces no parameter beyond what built \code{stage1},
and a caller-supplied value could silently disagree with that. \code{stage1} must
therefore come from an \code{ld_complexity_reduction()} new enough to record
\code{params} (added alongside this function); an older \code{stage1} object errors
rather than guessing.
}
\seealso{
\code{\link[=ld_unit_matrix]{ld_unit_matrix()}}, \code{\link[=ld_outlier_perm]{ld_outlier_perm()}}, \code{\link[=ld_region_rotation]{ld_region_rotation()}},
\code{\link[=ld_scan]{ld_scan()}} (the C-score alternative), \code{\link[=ld_complexity_reduction]{ld_complexity_reduction()}}
}
