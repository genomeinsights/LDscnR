% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/ld_benchmark.R
\name{flag_true_qtns}
\alias{flag_true_qtns}
\title{Flag true-positive QTNs in a simulated map}
\usage{
flag_true_qtns(
  map,
  va_col = "Va",
  maf_col = "MAF",
  maf_min = 0.1,
  p_va_min = 0.05
)
}
\arguments{
\item{map}{A data.table with (at least) \code{type} (\code{"QTN"} for QTNs), \code{Chr}, a
MAF column and a per-QTN additive-variance column.}

\item{va_col, maf_col}{Column names for additive variance and MAF.}

\item{maf_min}{Minimum MAF for a QTN to be detectable (default 0.1).}

\item{p_va_min}{Minimum per-chromosome Va share for a QTN to count as a true
positive (default 0.05).}
}
\value{
\code{map} (copied) with \code{true_pos_QTN} (and helper \code{sum_Va_ok}/\code{p_Va_ok}).
}
\description{
Marks which simulated QTNs count as detectable "true positives": a QTN passing
a minor-allele-frequency floor whose per-chromosome share of additive variance
(\code{Va / sum(Va)} over the chromosome's QTNs) is at least \code{p_va_min}. Sets a
logical column \code{true_pos_QTN} used throughout the benchmark.
}
