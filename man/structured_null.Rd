% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/structured_null.R
\name{structured_null}
\alias{structured_null}
\title{Structure-aware null for tau_C calibration}
\usage{
structured_null(
  y,
  GTs,
  K,
  ld_ws,
  basis = c("genetic", "spatial"),
  coords = NULL,
  B = 100L,
  alpha = 0.05,
  rho = colnames(ld_ws),
  qstar = seq(0, 0.95, by = 0.05),
  prep = NULL,
  seed = 1L
)
}
\arguments{
\item{y}{Observed phenotype vector (length = number of individuals).}

\item{GTs}{Genotype dosage matrix (individuals x SNPs; column names = markers).}

\item{K}{Kinship / GRM used both to correct EMMAX and as the genetic structure
basis. For empirical data prefer a GRM built from LD-independent markers so it
captures structure without absorbing the signal.}

\item{ld_ws}{Local-LD support matrix for the C-score (see \code{\link[=ld_cscore]{ld_cscore()}}).}

\item{basis}{Structure basis, \code{"genetic"} (default) or \code{"spatial"}.}

\item{coords}{Two-column matrix/data.frame of coordinates (required for
\code{basis = "spatial"}).}

\item{B}{Number of surrogates (default 100).}

\item{alpha, rho, qstar}{C-score parameters passed to \code{\link[=ld_cscore]{ld_cscore()}}.}

\item{prep}{Optional \code{emmax_prep} from \code{\link[=emmax_setup]{emmax_setup()}} (reused by
\code{\link[=ld_outlier_regions]{ld_outlier_regions()}} to avoid re-rotating the genotypes); built from
\code{GTs}, \code{K} if \code{NULL}.}

\item{seed}{RNG seed for reproducible surrogates.}
}
\value{
An \code{ld_null} object: \code{C_obs} (the full observed per-SNP C-vector),
\code{C_surr} (list of \code{B} \emph{sparse} surrogate C-vectors, only \code{C > 0}),
\code{universe} (every marker the observed or any surrogate lights up), and the
settings. Build an \code{\link[=ld_edges]{ld_edges()}} over \code{universe}, then call \code{\link[=null_fdr]{null_fdr()}} /
\code{\link[=calibrate_tauc]{calibrate_tauc()}}.
}
\description{
Draws \code{B} surrogate phenotypes that keep the observed phenotype's \emph{structure}
(its spatial or genetic autocorrelation) but are \emph{orthogonal} to it, runs a
fast EMMAX scan for each (\code{\link[=emmax_fast]{emmax_fast()}}), and stores the resulting per-SNP
C-scores (\code{\link[=ld_cscore]{ld_cscore()}}). Because a surrogate reproduces the structure-driven
false-positive rate while carrying none of the true signal, the empirical FDR
of the C-score against these surrogates (\code{\link[=null_fdr]{null_fdr()}}, \code{\link[=calibrate_tauc]{calibrate_tauc()}})
gives a truth-free \code{tau_C} that prices in structure confounding -- unlike a
naive phenotype permutation, which breaks the structure<->phenotype link and
is too permissive.
}
\details{
Surrogates keep the structure but are made orthogonal to \code{y} by Gram-Schmidt
(\code{resid(lm(surrogate ~ y))}):
\itemize{
\item \code{basis = "genetic"}: \code{surrogate ~ MVN(0, K)} -- same genetic-relatedness
covariance as the observed structure (use when confounding is genetic, e.g.
ecotype divergence).
\item \code{basis = "spatial"}: \verb{surrogate ~ MVN(0, Gaussian kernel over coords)} --
same spatial autocorrelation (use when confounding is spatial, e.g. an
environmental gradient); requires \code{coords}.
}
}
\seealso{
\code{\link[=ld_null_from_p]{ld_null_from_p()}} to build this bundle from your own p-values,
\code{\link[=ld_gate]{ld_gate()}}, \code{\link[=ld_region_scan]{ld_region_scan()}}, \code{\link[=ld_region_c2]{ld_region_c2()}}
}
