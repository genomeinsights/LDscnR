% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/emmax.R
\name{emmax}
\alias{emmax}
\title{EMMAX single-locus association test accounting for relatedness}
\usage{
emmax(Y, X, K, B = NULL, binary = FALSE, Covar = NULL, cores = 1)
}
\arguments{
\item{Y}{Numeric phenotype vector (length \code{n} individuals).}

\item{X}{Numeric genotype matrix, individuals (rows) by markers/units
(columns), coded as dosages.}

\item{K}{Pre-computed \code{n} by \code{n} kinship / genomic relationship matrix.}

\item{B}{Optional number of permutations for a permutation-adjusted p-value.
\code{NULL} (default) skips permutation; if set, the \code{MASS} package is required.}

\item{binary}{Logical; if \code{TRUE} and \code{B} is set, permutation phenotypes are
dichotomised to match the case/control ratio of \code{Y}.}

\item{Covar}{Optional covariate; if it varies, \code{Y} is replaced by the
residuals of \code{lm(Y ~ Covar)} before testing.}

\item{cores}{Number of cores for the permutation loop.}
}
\value{
A list with \code{F} (per-column F statistics), \code{pval} (F-test p-values),
\code{Rsq} (per-column \eqn{R^2}), and -- when \code{B} is supplied -- \code{pval.corr}
(permutation-adjusted p-values).
}
\description{
Efficient mixed-model association (EMMAX) for a quantitative or binary
phenotype, correcting for genome-wide relatedness through a kinship matrix
\code{K}. This is a natural downstream use of the LD-pruned representative marker
set produced by \code{\link[=ld_prune_and_eMLG]{ld_prune_and_eMLG()}}: build \code{K} from the pruned markers
(e.g. \code{SNPRelate::snpgdsGRM}) and then test individual markers, cluster
representatives, or eMLG consensus dosages for association.
}
\references{
Li Z, Kemppainen P, Rastas P, Merila J (2018). Linkage disequilibrium
clustering-based approach for association mapping with tightly linked
genome-wide data. \emph{Molecular Ecology Resources}. Original implementation
by Zitong Li.
}
