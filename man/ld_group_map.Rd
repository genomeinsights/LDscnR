% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/ld_group_map.R
\name{ld_group_map}
\alias{ld_group_map}
\title{Expand cluster memberships to a long marker-to-group table}
\usage{
ld_group_map(groups, prefix = NULL, sep = "_", cols = NULL)
}
\arguments{
\item{groups}{Either the \code{groups} element of an \code{\link[=ld_prune_and_eMLG]{ld_prune_and_eMLG()}} result,
or the whole result (the \code{groups} element is taken from it). Any table with
a list-column \code{members} and one row per group will do.}

\item{prefix}{Optional scalar prepended to \code{group_id}, separated by \code{sep}.
Group ids are unique only within a run, so when several chromosomes or files
are pooled the ids collide; passing the file or chromosome makes them unique
without a second pass.}

\item{sep}{Separator used with \code{prefix}. Default \code{"_"}.}

\item{cols}{Additional column names of \code{groups} to carry through, recycled to
the marker rows. \code{n_loci} and \code{Chr} are the usual ones.}
}
\value{
A \code{data.table} with one row per marker: \code{marker}, \code{group_id}, and any
columns named in \code{cols}. Marker order follows \code{groups$members}.
}
\description{
\code{\link[=ld_prune_and_eMLG]{ld_prune_and_eMLG()}} returns its groups with a list-column \code{members}, one
character vector of marker names per group. Nearly every downstream step needs
the inverse of that -- one row per marker, carrying its group -- so it can be
joined to a map or a vector of per-marker statistics.
}
\details{
The obvious way to build it is a loop over groups:

\if{html}{\out{<div class="sourceCode r">}}\preformatted{# DON'T: allocates one data.table per group
rbindlist(lapply(seq_len(nrow(g)), function(k)
  data.table(marker = g$members[[k]], group_id = g$group_id[k])))
}\if{html}{\out{</div>}}

That is quadratic-ish in practice and measurably slow: on a 13,673-group
chromosome it takes 0.94 s against 0.01 s for the vectorised form below, a
157-fold difference, and roughly a fifth of the cost of the clustering it
follows. This function exists so the fast form is the one that comes to hand.
}
\examples{
g <- data.frame(group_id = c("A", "B"), n_loci = c(2L, 1L))
g$members <- list(c("m1", "m2"), "m3")
ld_group_map(g)
ld_group_map(g, prefix = 7, cols = "n_loci")

}
\seealso{
\code{\link[=ld_prune_and_eMLG]{ld_prune_and_eMLG()}}
}
