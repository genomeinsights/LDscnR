% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/plot_genotype_heatmap.R
\name{plot_genotype_heatmap}
\alias{plot_genotype_heatmap}
\title{Plot a genotype dosage heatmap with optional column/row annotations}
\usage{
plot_genotype_heatmap(
  GTs,
  polarize = TRUE,
  cor_threshold = 0,
  cluster_columns = TRUE,
  col_cluster_method = "single",
  row_order = NULL,
  cluster_rows = is.null(row_order),
  col_annotation = NULL,
  col_annotation_colors = NULL,
  col_annotation_name = "annotation",
  row_annotation = NULL,
  row_annotation_colors = NULL,
  row_annotation_name = "annotation",
  geno_colors = c("#2166AC", "#F7F7F7", "#B2182B"),
  legend_name = if (polarize) "genotype (polarized)" else "genotype",
  title = NULL,
  out_file = NULL,
  width = 10,
  height = 7,
  ...
)
}
\arguments{
\item{GTs}{Individuals (rows) x markers (cols) dosage matrix (0/1/2),
already restricted to the markers/individuals of interest (e.g. one
\code{\link[=ld_prune_and_eMLG]{ld_prune_and_eMLG()}} group's members).}

\item{polarize}{Logical; if \code{TRUE} (default), flips (2 - x) any marker
negatively correlated with the most-complete reference marker via
\code{polarize_genotypes()} before display, so a real LD block renders as a
visually coherent stripe rather than a checkerboard caused purely by
strand/reference-allele choice. Purely cosmetic -- r^2 (and so any
distance/clustering computed on it) is sign-invariant, so this never
changes \code{cluster_columns}, only what's drawn.}

\item{cor_threshold}{Passed to \code{polarize_genotypes()}'s \code{cor_threshold}.}

\item{cluster_columns}{Logical; hierarchically cluster markers (columns)
on 1 - r^2 pairwise LD, computed on the unpolarized genotypes (identical
either way, since r^2 is sign-invariant). Default \code{TRUE}.}

\item{col_cluster_method}{Linkage method for column clustering, passed to
\code{stats::hclust()}. Default \code{"single"}, consistent with
\code{\link[=ld_complexity_reduction]{ld_complexity_reduction()}}'s own Stage 1 approach.}

\item{row_order}{Optional explicit character vector of \code{rownames(GTs)}
(e.g. individuals ordered by population), used verbatim and taking
precedence over \code{cluster_rows}. Useful for keeping one consistent
individual order across many figures.}

\item{cluster_rows}{Logical; hierarchically cluster individuals (rows) on
Euclidean genotype distance. Defaults to \code{TRUE} only when \code{row_order} is
\code{NULL}.}

\item{col_annotation, row_annotation}{Optional named vector or factor giving
a categorical grouping to show as a colour bar above (\code{col_annotation},
names = markers) or to the left (\code{row_annotation}, names = individuals)
of the heatmap. Every id in \code{colnames(GTs)} / \code{rownames(GTs)} (after any
\code{row_order} reordering) must have an entry.}

\item{col_annotation_colors, row_annotation_colors}{Optional named colour
vector (names = levels of the corresponding annotation). Auto-generated
from \code{grDevices::hcl.colors()} if not supplied.}

\item{col_annotation_name, row_annotation_name}{Legend/axis label for the
respective annotation. Default \code{"annotation"}.}

\item{geno_colors}{Length-3 colour vector for dosage 0/1/2, passed to
\code{circlize::colorRamp2()}. Default low-mid-high diverging blue/white/red.}

\item{legend_name}{Heatmap legend title. Defaults to
\code{"genotype (polarized)"} / \code{"genotype"} depending on \code{polarize}.}

\item{title}{Optional \code{column_title} for the heatmap.}

\item{out_file}{Optional path to save the heatmap as a PNG (directory
created if missing). If \code{NULL} (default), nothing is saved -- draw the
returned object yourself, e.g. \code{ComplexHeatmap::draw(ht)}.}

\item{width, height}{PNG dimensions in inches, used only when \code{out_file} is
given.}

\item{...}{Additional arguments passed through to \code{ComplexHeatmap::Heatmap()}.}
}
\value{
The \code{Heatmap} object, invisibly. Printing/drawing it (automatic at
the console, or via \code{ComplexHeatmap::draw()}) renders the figure;
\code{out_file} (if given) has already been saved as a side effect.
}
\description{
A \code{ComplexHeatmap::Heatmap()} of a genotype dosage matrix (markers x
individuals), with optional colour-bar annotations for both dimensions --
e.g. Stage-1 LD-cluster id per marker, population per individual -- and an
optional fixed row order. Built to answer the question \code{plot_pruning_comparison()}
can't: whether an apparent LD block is one coherent signal across all
individuals, or is instead driven by a subset (e.g. one population being
different from everything else).
}
\examples{
\dontrun{
grp <- result$groups[group_id == "F3968"]
mk <- grp$members[[1]]

pop_order <- sample_data[order(Population), Sample_ID]
cl_by_marker <- setNames(map_snp$CL_id, map_snp$marker)
pop_by_ind <- setNames(sample_data$Population, sample_data$Sample_ID)

ht <- plot_genotype_heatmap(
  GTs[, mk],
  row_order = pop_order,
  col_annotation = cl_by_marker[mk], col_annotation_name = "Stage 1 cluster",
  row_annotation = pop_by_ind[pop_order], row_annotation_name = "Population",
  title = "Chr2: F3968", out_file = "./Figures/Chr2_F3968_heatmap.png"
)
}

}
